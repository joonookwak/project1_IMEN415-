---
title: "group_project"
output:
  word_document: default
  pdf_document: default
date: "2026-04-30"
---
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo=TRUE, warning=FALSE, message=FALSE)
knitr::opts_knit$set(root.dir = 
  "/Users/kwakjoonoo/Desktop/Multivariate_Analysis")
```

```{r}
traindir <- "/Users/kwakjoonoo/Desktop/Multivariate_Analysis/Train Data"
sourcedir <-"/Users/kwakjoonoo/Desktop/Multivariate_Analysis"
system("git remote -v")
setwd(sourcedir)
source("AccidentInput.R")

library(ggplot2)
library(dplyr)
# Load a list of data frames for each year of accident data
acts <- file.inputl(traindir)

# combine into one data frame
totacts <- combine.data(acts)

# setup categorical variables
totacts$TYPE <- factor(totacts$TYPE, labels = c("Derailment", "HeadOn", 
                                                "Rearend", "Side", "Raking", "BrokenTrain", "Hwy-Rail", 
                                                "GradeX", "Obstruction", "Explosive", "Fire","Other",
                                                "SeeNarrative" ))

totacts$TYPEQ <- sub("\\.0$", "", totacts$TYPEQ)
totacts$TYPEQ[totacts$TYPEQ %in% c("O", "")] <- ""
totacts$TYPEQ <- factor(totacts$TYPEQ, labels = c("NA", "Freight", "Passenger", "Commuter", 
                                                  "Work",  "Single", "CutofCars", "Yard", "Light", "Maint",
                                                  "MaintOfWay", "Passenger", "Commuter", "ElectricMulti", "ElectricMulti"))

totacts$Cause <- rep(NA, nrow(totacts))

totacts$Cause[which(substr(totacts$CAUSE, 1, 1) == "M")] <- "M"
totacts$Cause[which(substr(totacts$CAUSE, 1, 1) == "T")] <- "T"
totacts$Cause[which(substr(totacts$CAUSE, 1, 1) == "S")] <- "S"
totacts$Cause[which(substr(totacts$CAUSE, 1, 1) == "H")] <- "H"
totacts$Cause[which(substr(totacts$CAUSE, 1, 1) == "E")] <- "E"

totacts$Cause <- factor(totacts$Cause)

totacts$Casualties <- totacts$TOTINJ + totacts$TOTKLD
# 중복 제거
totacts_nd<- totacts[!(duplicated(totacts[, c("INCDTNO", "YEAR", "MONTH", "DAY", "TIMEHR", "TIMEMIN")])),]

# 과제 대상 data frmae
dmgbox <- ggplot(totacts_nd, aes(y = ACCDMG)) + geom_boxplot()
upper  <- ggplot_build(dmgbox)$data[[1]]$ymax

df_accdmg     <- totacts_nd %>% filter(ACCDMG > upper)
df_casualties <- totacts_nd %>% filter(Casualties >= 1)

```



```{r}
#ACCDMG 가설 1
library(geosphere)
library(ggplot2)
library(dplyr)
library(maps)

library(ggplot2)
library(dplyr)
library(geosphere) 

# ==========================================
# 극단치 기준 적용
# ==========================================


# ==========================================
# 도심 접근성 (LOCATION_TYPE) 변수 생성
# ==========================================
# 주요 도시 리스트 유지
major_cities <- data.frame(
  city = c("New York", "Chicago", "Los Angeles", "Houston", "Philadelphia", 
           "Phoenix", "San Antonio", "San Diego", "Dallas", "San Jose",
           "Austin", "Jacksonville", "San Francisco", "Columbus", "Charlotte",
           "Indianapolis", "Seattle", "Denver", "Washington D.C.", "Boston",
           "Detroit", "Nashville", "Atlanta", "Miami", "Minneapolis"),
  lat = c(40.71, 41.87, 34.05, 29.76, 39.95, 33.44, 29.42, 32.71, 32.77, 37.33,
          30.26, 30.33, 37.77, 39.96, 35.22, 39.76, 47.60, 39.73, 38.90, 42.36,
          42.33, 36.16, 33.74, 25.76, 44.97),
  lon = c(-74.00, -87.62, -118.24, -95.36, -75.16, -112.07, -98.49, -117.16, -96.79, -121.88,
          -97.74, -81.65, -122.41, -82.99, -80.84, -86.15, -122.33, -104.99, -77.03, -71.05,
          -83.04, -86.78, -84.38, -80.19, -93.26)
)

# 거리 계산 함수
get_min_dist <- function(lon, lat, city_df) {
  if(is.na(lon) | is.na(lat)) return(NA)
  dists <- distHaversine(c(lon, lat), city_df[, c("lon", "lat")])
  return(min(dists) / 1000) 
}

df_accdmg$dist_to_city <- mapply(get_min_dist, 
                                 df_accdmg$Longitud, 
                                 df_accdmg$Latitude, 
                                 MoreArgs = list(city_df = major_cities))

# 반경 설정 
radius_threshold <- 200
df_accdmg <- df_accdmg %>%
  filter(!is.na(dist_to_city)) %>% 
  mutate(LOCATION_TYPE = ifelse(dist_to_city <= radius_threshold, 
                                "Urban (Accessible)", 
                                "Isolated (Rural)"),
         LOCATION_TYPE = as.factor(LOCATION_TYPE))

```


## ACCDMG Hypothesis 1: location × TRNSPD
### Hypothesis Statement

탈선 사고 발생 시, 열차 속도(TRNSPD)가 피해액(ACCDMG)을 증가시키는 정도는 사고 발생 지역의 도심 접근성(LOCATION_TYPE)에 따라 유의미한 차이(교호작용)를 보일 것이다 
- "도심과 멀어질수록 큰 피해액을 보일 것이다"

---
#이론적 근거
**(1) Interaction Effect of Location Type and Train Speed **
열차 사고에서 선로의 위치적 특성(Location/Area type)과 열차의 주행 속도(Train speed)는 독립적으로 작용하는 것을 넘어, 결합되었을 때 사고의 피해 규모를 극대화한다. Hao and Daniel (2014)의 연구에 따르면, 열차 속도와 지역 유형(Area type)은 능동 및 수동 철도 건널목 모두에서 운전자 상해 심각도에 영향을 미치는 핵심적인 다중 요인으로 식별되었다
. 특히 도심지가 아닌 외곽의 농촌 지역(Rural areas)이나 개방된 공간에서는 열차가 더 빠른 속도로 달리기 쉬우며, 이처럼 높은 열차 속도와 농촌 지역이라는 환경적 조건이 맞물렸을 때 치명적인 부상과 심각한 피해가 발생할 확률이 급증한다(Khan & Khattak, 2018; Zhao et al., 2019)
. 더불어 선로의 접근성 및 유형 측면에서 Ahmed et al. (2023)은 주본선(Main tracks)에서 발생하는 충돌 사고가 차량기지(Yard), 측선(Siding) 또는 산업 구역에서 발생하는 사고에 비해 부상 및 사망 가능성이 훨씬 높음을 실증하였다
. 이는 열차가 고속을 유지할 수 있는 주본선이나 개방된 지역이라는 '위치적 특성(LOCATION_TYPE)'과 높은 '열차 속도(TRNSPD)'가 교호작용할 때, 시스템의 물리적 충격이 극대화되어 사고 심각도가 폭증함을 의미한다.

인용: Hao, W., & Daniel, J. (2014). Motor vehicle driver injury severity study under various traffic control at highway-rail grade crossings in the United States. Journal of Safety Research, 51, 41–48.; Khan, W. A., & Khattak, A. J. (2018). Injury severity of truck drivers in crashes at highway-rail grade crossings in the United States. Transportation Research Record, 2672(10), 38–47.; Ahmed, S. S., Corman, F., & Anastasopoulos, P. C. (2023). Accounting for unobserved heterogeneity and spatial instability in the analysis of crash injury-severity at highway-rail grade crossings: A random parameters with heterogeneity in the means and variances approach. Analytic Methods in Accident Research, 37, 100250.
---
**(2) Combined Effect of Area Type/Infrastructure and Train Speed **
운송 사고의 심각도는 차량의 단순한 물리적 속도뿐만 아니라, 사고가 발생한 지역의 인구 밀도 구역(도시, 교외, 농촌) 및 선로 주변의 고정된 환경적 특성(Fixed feature)과 상호작용할 때 기하급수적으로 증폭된다. 국제원자력기구(IAEA)의 운송 위험 평가 보고서(2003)에 인용된 미국 사고 심각도 범주 데이터(Table 1)에 따르면, 전체 사고의 발생 빈도 자체는 도시 지역(Urban)이 더 높게 나타나지만, 피해 규모는 미미한 수준에 그쳤다. 반면, 방해물이 적어 열차가 높은 속도를 유지하기 쉬운 농촌/외곽 지역(Rural)에서는 심각한 물리적 충격과 대형 화재를 동반하는 최상위 심각도(Category 6) 사고의 조건부 발생 확률(7.0E-6)이 도시 지역(1.3E-7)에 비해 무려 약 54배 이상 높게 나타났다
. 더불어 AEA의 확률론적 사고 평가 방법론에 따르면, 탈선이나 충돌 시 발생하는 피해(ACCDMG)는 열차의 주행 속도 그 자체보다 높은 속도 에너지를 지닌 열차가 탈선 후 교량 교대(bridge abutment), 터널, 선로 변 구조물 등 '특정 경로 상의 고정된 인프라(Fixed feature of the transport route)'와 측면으로 상호작용(충돌)할 때 파괴적 손상으로 직결된다고 명시하고 있다
. 이는 도심 외부의 주본선이나 개방된 지역 등 열차가 고속으로 주행하는 특정 위치 환경(LOCATION_TYPE)과 열차 속도(TRNSPD)가 결합할 때, 시스템의 파괴력이 극대화되어 극단적인 사고 피해액을 초래한다는 교호작용 가설을 강력하게 뒷받침한다.
인용: International Atomic Energy Agency. (2003). Input data for quantifying risks associated with the transport of radioactive material (IAEA-TECDOC-1346). IAEA, Vienna.
--- 
# Expectation
동일하게 고속으로 탈선하더라도, 복구 장비와 비상 대응 인력의 투입이 신속한 도심 인근(Urban)에 비해, 인프라가 부족한 고립된 외곽 지역(Rural)에서 속도 증가에 따른 피해액 폭증이 더 가파를 것으로 예상된다.
---
# feature creation
공간 거리 계산: 미국 내 상위 25개 주요 대도시의 실제 위도(Latitude)와 경도(Longitud) 좌표를 기준으로, 각 극단적 사고 발생 지점까지의 최단 구면 거리를 산출하였다.

범주형 파생 변수(LOCATION_TYPE) 생성: 도출된 최단 거리를 기준으로, 도심으로부터 반경 200km 이내에서 발생한 사고는 'Urban (Accessible, 1)', 그 외의 외곽 지역에서 발생한 사고는 'Isolated (Rural, 0)'로 분류하여 교호작용 모델에 투입할 질적 변수(Qualitative Variable)를 새롭게 정의하였다.

```{r}
# 지도 시각화 (Visualization)
# ==========================================
states_map <- map_data("state")

ggplot() +
  # 배경 지도 (미국 본토)
  geom_polygon(data = states_map, aes(x = long, y = lat, group = group),
               fill = "gray90", color = "white") +
  
  # 기준점: 주요 도시 (검은색 X 표시)
  geom_point(data = major_cities, aes(x = lon, y = lat), 
             color = "black", shape = 4, size = 3, stroke = 1.2) +
  
  # 데이터점: 극단치 사고 발생 위치
  geom_point(data = df_accdmg, 
             aes(x = Longitud, y = Latitude, color = LOCATION_TYPE), 
             size = 2.5, alpha = 0.8) +
  
  # 디자인 스케일 및 색상 지정
  coord_fixed(1.3, xlim = c(-125, -65), ylim = c(24, 50)) +
  scale_color_manual(values = c("Isolated (Rural)" = "tomato", 
                                "Urban (Accessible)" = "steelblue")) +
  theme_minimal() +
  labs(title = "Extreme Damage Accidents (Above Upper Whisker)",
       subtitle = paste0("Categorized by Proximity to Major Cities (", radius_threshold, "km radius)"),
       x = "Longitude", y = "Latitude", color = "Location Type") +
  theme(legend.position = "bottom",
        plot.title = element_text(face = "bold", size = 14),
        plot.subtitle = element_text(color = "darkgray"))

```
새롭게 생성된 공간 변수를 바탕으로, 상위 극단치 사고들의 실제 발생 위치를 미국 본토 지도 위에 시각화하였다.

시각화 결과 분석: 지도를 통해 분석한 결과, 극단적인 피해를 입힌 탈선 사고들의 분포는 Rural(주황색)과 Urban(파란색)이 약 3:1의 비율로 나타났다.
---
이는 주요 물류 허브(Urban)를 연결하는 광활한 장거리 횡단 선로(Rural)에서 고속 주행 중 발생하는 대형 사고의 빈도가 높음을 시사한다. 이 분류된 공간 데이터를 활용하여, 예정된 다중 선형 회귀 교호작용 모델을 구축하고 가설의 통계적 유의성을 검증할 계획이다.

```{r}

# 1. 시각화를 위한 데이터 준비 (결측치 및 0값 제거)
plot_data <- df_accdmg %>%
  filter(!is.na(TRNSPD) & !is.na(ACCDMG) & ACCDMG > 0 & !is.na(LOCATION_TYPE))

# 2. 교호작용 그래프 (Interaction Plot) 생성
ggplot(plot_data, aes(x = TRNSPD, y = log(ACCDMG), color = LOCATION_TYPE, fill = LOCATION_TYPE)) +
  
  # 실제 데이터 포인트 반투명하게 plot
  geom_point(alpha = 0.4, size = 2) +
  
  # 회귀선 (선형 추세선) 추가가
  geom_smooth(method = "lm", se = TRUE, alpha = 0.2) +
  
  scale_color_manual(values = c("Isolated (Rural)" = "tomato", 
                                "Urban (Accessible)" = "steelblue")) +
  scale_fill_manual(values = c("Isolated (Rural)" = "tomato", 
                               "Urban (Accessible)" = "steelblue")) +
  
  theme_minimal() +
  labs(title = "Interaction Effect: Speed vs. Damage by Location Type",
       subtitle = "Focusing on Extreme Damage Accidents (ACCDMG > Upper Whisker)",
       x = "Train Speed (TRNSPD, mph)",
       y = "Log of Accident Damage (log(ACCDMG))",
       color = "Location Type",
       fill = "Location Type") +
  theme(legend.position = "bottom",
        plot.title = element_text(face = "bold", size = 14),
        plot.subtitle = element_text(color = "darkgray"))
```

interaction plot을 보면 어느정도의 interaction이 있음이 확인된다. 즉, TRNSPD 가 커질수록 rural지역에서의 ACCDMG(log scale-임의)값이 커짐을 알 수 있다. 


---
#2. Y distribution check

```{r}
ggplot(df_accdmg, aes(x = ACCDMG)) +
  geom_density(fill = "steelblue", alpha = 0.5) +
  labs(title = "Distribution of ACCDMG",
       x = "ACCDMG", y = "Density") +
  theme_bw()

```

ACCDMG는 양의 정수만 취하는 이산형 변수이며 right-skew의 형태를 띤다.
정규성과 등분산성 가정을 충족하기 어려우므로 Box-Cox 변환이 필요해보인다.

---
# step3. Linear Models

우선 box-cox 변환을 하지 않고 original y에 대해서 linear 모델을 만들어보겠다.

```{r}
# main effect
ACC2.lm1 <- lm(ACCDMG ~ LOCATION_TYPE + TRNSPD, data = df_accdmg)
summary(ACC2.lm1)
```

속도의 영향 (TRNSPD): 계수(Estimate)가 약 17,595이며 p-value가 매우 유의미(< 2e-16)합니다. 즉, 지역에 상관없이 열차 속도가 1mph 증가할 때마다 피해액이 평균적으로 약 $17,595씩 증가합니니다.

지역의 영향 (LOCATION_TYPEUrban): 계수가 91,907이고 p-value가 0.00212로 유의미합니다. 기준점인 외곽(Rural)에 비해, 속도가 동일하다는 가정하에 도심(Urban)에서 발생한 사고의 기본 피해액이 평균적으로 약 $91,907 더 큽니다. 이는 location_type이 significant하지만 가설과 반대되는 방향의 insight를 제공합니다.(도심지역의 ACCDMG가 더 클 것이다다)

모델 설명력 (R-squared): 약 0.064 (6.4%)로 상당히 낮습니다. 극단적인 아웃라이어들이 많아서 선형 모델 하나로 설명하기 벅찬 상태입니다.


```{r}
# main effect + interaction effect
ACC2.lm2 <- lm(ACCDMG ~ LOCATION_TYPE * TRNSPD, data = df_accdmg)
summary(ACC2.lm2)
```

교호작용 항 : 계수는 -1608이지만, p-value가 0.31756으로 0.05보다 훨씬 커서 통계적으로 유의미하지 않습니다.

해석: 이 모델만 보면 "도심이든 시골이든 속도가 피해액을 증가시키는 정도(기울기)에는 통계적으로 의미 있는 차이가 없다"는 결론이 나옵니다.

```{r}
# main effect + interaction effect (second order)
ACC2.lm3 <- lm(ACCDMG ~ LOCATION_TYPE * TRNSPD + I(TRNSPD^2), data = df_accdmg)

# 2차항 모델 결과 확인
summary(ACC2.lm3)

```

해석 :

2차항 : 속도의 제곱에 비례하여 피해액이 기하급수적으로 커진다는 사실이 증명되었습니다. (물리적 에너지 법칙과 일치) 

교호작용 항: 도심에서는 속도 증가에 따른 피해액의 상승 폭이 시골에 비해 유의미하게 낮음을 의미합니다.

열차 속도와 사고 피해액은 단순 선형이 아닌 2차 함수 관계($TRNSPD^2$)를 가짐이 입증되었다. 특히 도심 접근성과의 교호작용 계수가 음수(-3,619.2)로 유의미하게 산출된 점은 주목할 만하다. 

#### Partial F-test:

```{r }
anova(ACC2.lm1, ACC2.lm2, ACC2.lm3)
```


**2차항의 물리적 증명 **
(I(TRNSPD^2))계수가 188.7이고 p-value가 7.36e-09 ()* 로 매우 유의미합니다.
해석: 탈선 피해액은 속도에 단순히 정비례(1차 선형)해서 커지는 게 아니라, 운동 에너지 공식($v^2$)처럼 속도의 제곱에 비례하여 기하급수적으로 폭증한다는 사실이 증명된다.
**interaction term**
아까 Model 2(1차항만 있던 모델)에서는 이 교호작용 p-value가 0.317로 기각되었었다.? 그런데 2차항을 넣은 지금, p-value가 0.02764 (*)로 떨어지며 통계적으로 유의미해졌다!!

#### Model Comparison (AIC / Adj. R²)

```{r}
# model 3개 AdjR2, AIC, BIC 비교
model_comparison <- data.frame(
  Model = c("ACC2.lm1 (Main)", "ACC2.lm2 (Interaction)", "ACC2.lm3 (Second Order)"),
  AdjR2 = round(c(summary(ACC2.lm1)$adj.r.squared,
                  summary(ACC2.lm2)$adj.r.squared,
                  summary(ACC2.lm3)$adj.r.squared), 5),
  AIC   = round(c(AIC(ACC2.lm1), AIC(ACC2.lm2), AIC(ACC2.lm3)), 2),
  BIC   = round(c(BIC(ACC2.lm1), BIC(ACC2.lm2), BIC(ACC2.lm3)), 2)
)

# 결과 표 출력
print(model_comparison)
```
표를 봤을때 lm3을 채택하여야한다 - AdjR2,AIC,BIC 모든 측면에서 우수.

하지만 이 모델들들은 model adequacy가 확인되지 않은 상태에서 진행되었기 때문에 의미있는 통계는 아니다.
추가로 Y에 대해 log transformation을 한 결과 interaction이 확인되었기 때문에 box-cox transformation이후 interaction이 존재할것으로 보인다. 
따라서 가장 performance가 좋은 lm3에 대해서 model adequacy를 확인해보겠다.

---
#step4. model diagnostics

```{r}
library(ggfortify)
autoplot(ACC2.lm3, which = c(1, 2, 3, 5), ncol = 2, label.size = 3) +
  theme_bw() +
  ggtitle("Diagnostics: ACC2.lm3 (Untransformed)")
```
잔차 진단에서 다음이 관찰된다.

- **Residuals vs Fitted**: 잔차가 0 근처에 몰리고 추세선의 기울기가 0에 가까움 -> 선형성 판단 어려움움
- **Normal Q-Q**: 오른쪽 꼬리가 심하게 벗어남 → 정규성 위반
- **Scale-Location**: 추세선 우상향 → 등분산성 위반
- **Residuals vs Leverage**: 표준화 잔차 ±3 초과 극단치 다수 존재

정규성,등분산성 위배, 다수의 outlier 관찰됨 -> 앞선 linear model의 통계값들은 신뢰성이 부족하다. 

=> box-cox transformation을 진행한다.


---
#Step5. Box-Cox transformation

```{r}
library(MASS)
# 1. lm3 구조에 대한 최적의 Box-Cox Lambda(람다) 찾기
# 2차항(I(TRNSPD^2))이 포함된 구조에서 최적의 변환 지수를 계산합니다.
bc_result <- boxcox(ACCDMG ~ LOCATION_TYPE * TRNSPD + I(TRNSPD^2), 
                    data = df_accdmg, 
                    lambda = seq(-2, 2, 1/10), plotit = TRUE)

# 2. 최적의 람다 추출
opt_lambda <- bc_result$x[which.max(bc_result$y)]
cat("최적의 Lambda 값:", opt_lambda, "\n")

# 3. 데이터 변환 적용
df_accdmg$ACCDMG_trans <- ((df_accdmg$ACCDMG ^ opt_lambda) - 1) / opt_lambda
cat("최적의 Lambda", opt_lambda, "를 사용하여 Box-Cox 변환을 적용합니다.\n")

# 4. 변환된 Y값으로 Second-order 교호작용 모델(lm3 버전) 적합
ACC2_trans_m3 <- lm(ACCDMG_trans ~ LOCATION_TYPE * TRNSPD + I(TRNSPD^2), data = df_accdmg)

# 5. 최종 결과 및 진단 확인
ACC2_trans_m3 <- lm(ACCDMG_trans ~ LOCATION_TYPE * TRNSPD + I(TRNSPD^2), data = df_accdmg)

```

```{r}
autoplot(ACC2_trans_m3, which = c(1, 2, 3, 5), ncol = 2, label.size = 3) +
  theme_bw() +
  ggtitle("Diagnostics: ACC2_trans_m3 (transformed)")
```


"종속변수(ACCDMG)에 대한 Box-Cox 변환을 수행한 후 잔차 진단(Residual Diagnostics)을 재실행한 결과, 모델의 기본 가정 위배 문제가 성공적으로 해결되었음을 확인하였다.

Q-Q Plot 확인 결과, 극심했던 우측 편향(Right-skewed)이 해소되고 잔차가 정규분포의 형태를 강하게 띠어 정규성 가정을 충족하였다.

Residuals vs Fitted 및 Scale-Location 플롯에서 잔차의 흩어짐이 안정화되어 이분산성(Heteroscedasticity)이 통제되었음을 확인하였다.

레버리지 플롯 결과, 표준화 잔차가 정상 범주 내로 축소되어 모델을 왜곡하던 극단적 이상치의 영향력이 소거되었다.

결론적으로, 변환된 교호작용 모델은 통계적 가정(Model Adequacy)을 훌륭히 만족하며, 해당 모델에서 산출된 p-value와 회귀계수는 통계적으로 매우 높은 신뢰성을 가진다.


```{r}
summary(ACC2_trans_m3)
```

**모델 적합도 및 통계적 유의성**
그 결과, 모델의 설명력($R^2$)이 기존 대비 약 2배 향상되었으며, 모델의 적합도 역시 매우 우수하게 나타났다 (F-statistic p-value < 2.2e-16).

**가설 2 (교호작용)의 최종 채택 **
핵심 검증 대상이었던 '도심 접근성과 열차 속도의 교호작용(LOCATION_TYPE * TRNSPD)'이 통계적으로 매우 유의미한 음(-)의 계수를 가짐이 입증되었다(p < 0.001). 이는 속도 증가에 따른 피해액의 폭증 현상이 인프라가 부족한 외곽(Rural) 지역에서 훨씬 더 가파르게 발생하며, 도심(Urban) 지역에서는 상대적으로 억제됨을 시사한다. 

**속도의 비선형적 영향력 (2차항의 역할)** 
곡률 포착: 속도의 2차항(I(TRNSPD^2)) 역시 p-value 2e-16 미만으로 매우 유의미하게 나타났다.

=> 피해액이 단순히 속도에 정비례하는 것이 아니라, 속도의 변화에 따라 피해 규모가 비선형적으로 변화하는 물리적 특성을 모델이 성공적으로 반영하고 있다. 

하지만 이 모델을 통해 가설을 입증하기 이전에 추가적으로 체크할게 있다.

---
#step 6. VIF check

```{r}
library(car)

# 최종 모델(ACC2_trans_m3)의 VIF 값 확인
vif(ACC2_trans_m3)
```

교호작용과 2차항 포함으로 인해 구조적으로 다중공선성이 나왔으며 , 모델의 타당성에는 문제가 없어 보이긴 한다.
하지만 더 엄밀히 보기 위해 centering 을 통해 multicollinearity를 해소해보겠다.


```{r}

# 속도(TRNSPD) 변수 Mean-Centering (평균 중심화) 적용
df_accdmg$TRNSPD_c <- scale(df_accdmg$TRNSPD, center = TRUE, scale = FALSE)


# Centered 변수로 최종 모델 재적합 (구조는 동일)
ACC2_trans_m3_c <- lm(ACCDMG_trans ~ LOCATION_TYPE * TRNSPD_c + I(TRNSPD_c^2), data = df_accdmg)

# VIF 체크
vif(ACC2_trans_m3_c)

# 모델 결과 요약 확인
summary(ACC2_trans_m3_c)
```


centering을 한결과 multicollinearity를 모두 해소했으며 통계적 유의성은 유지됨을 알 수 있다. 

---
#Step 7: cross-validation
이제 실제로 이 모델이 우수한지 cross-validation을 통해 알아보겠다. 
단, lm3 단독으로는 정말 우수한지 알 수 없기에 lm2와와 lm3와 비교해보겠다.
```{r}

library(boot)

# 공정한 조건 세팅 확인
# - Y: 두 모델 모두 동일한 Box-Cox 변환 변수 (ACCDMG_trans) 사용
# - X: 두 모델 모두 동일한 Centering 변수 (TRNSPD_c) 사용

# 난수 고정 (결과 재현을 위해)
set.seed(2026) 

# ---------------------------------------------------------
# [후보 1] Model 2 (2차항 없음) 
# ---------------------------------------------------------
# 공정한 비교를 위해 변환된 Y(ACCDMG_trans)와 중심화된 X(TRNSPD_c) 사용
cv_model2 <- glm(ACCDMG_trans ~ LOCATION_TYPE * TRNSPD_c, 
                 data = df_accdmg, 
                 family = gaussian) 

# 10-Fold CV 실행
err_m2 <- cv.glm(df_accdmg, cv_model2, K=10)

# ---------------------------------------------------------
# [후보 2] Model 3 (2차항 포함) 
# ---------------------------------------------------------
cv_model3 <- glm(ACCDMG_trans ~ LOCATION_TYPE * TRNSPD_c + I(TRNSPD_c^2), 
                 data = df_accdmg, 
                 family = gaussian)

# 10-Fold CV 실행
err_m3 <- cv.glm(df_accdmg, cv_model3, K=10)

# ---------------------------------------------------------
# 결과 출력 (delta 값 비교)
# ---------------------------------------------------------
# delta는 두 개의 값을 반환합니다. 첫 번째 값은 기본 CV 에러(PMSE)이고,
# 두 번째 값은 편향이 보정된 CV 에러입니다. 보통 첫 번째 값을 봅니다.
cat("=== 10-Fold Cross-Validation Error (PMSE) 비교 ===\n")
cat("[Model 2 (2차항 X)] CV Error (delta):", err_m2$delta[1], "\n")
cat("[Model 3 (2차항 O)] CV Error (delta):", err_m3$delta[1], "\n")
cat("==================================================\n")

```

err_m3$delta[1] (Model 3의 오차) 값이 err_m2$delta[1] (Model 2의 오차) 값보다 더 작게 나왔으므로,
2차항을 포함한 최종 모델(lm3)이 new 데이터에 대해서도 더 우수한 예측력을 가짐을 증명된다. 

결론:
**Insight 1: 외곽 지역(Rural)은 고속 탈선 사고의 "숨겨진 시한폭탄"이다.**
데이터의 증거: 교호작용(LOCATION_TYPE * TRNSPD) 분석 결과, 도심(Urban) 지역은 기본 피해액 자체는 크지만 속도 증가에 따른 피해액의 상승 폭은 상대적으로 둔화되는 반면(- 계수 도출), 외곽 지역(Rural)은 열차 속도가 올라갈수록 피해액이 통제 불가능한 수준으로 치솟는 가파른 기울기를 보였습니다.

FRA를 위한 정책 제언:

"자원 배분의 패러다임 전환:" 인구 밀도가 낮다는 이유로 외곽 지역의 선로 안전망 구축이 후순위로 밀려서는 안 됩니다.

외곽 지역에서 고속 주행이 빈번하게 일어나는 화물/여객 노선의 경우, 탈선 시 피해를 차단할 인프라(비상 정지 시스템, 방호벽, 빠른 긴급 출동 체계)가 도심보다 부족하여 '한 번 사고가 나면 대형 참사'로 이어집니다. FRA는 외곽 고속 주행 구간에 대한 예방적 안전 예산(Preventive Maintenance Budget)을 우선적으로 배정해야 합니다.

**Insight 2: 도심(Urban)의 응급 인프라는 훌륭한 "피해 억제기(Damper)" 역할을 하고 있다.**
데이터의 증거: 변수 중심화(Mean-Centering) 및 Box-Cox 변환 후 확인한 교호작용 계수의 음(-)의 방향성은, 도심 지역의 인프라 접근성(Accessible)이 고속 탈선의 파괴력을 일정 부분 상쇄하고 있음을 강력하게 시사합니다.

FRA를 위한 정책 제언:

"성공 모델의 이식:" 도심 지역에서 대형 탈선 사고가 발생했을 때 피해 규모를 억제하는 핵심 요인(빠른 119/응급구조대 접근성, 견고한 주변 방호 시설, 촘촘한 관제 시스템 등)이 무엇인지 정밀 분석해야 합니다.

이를 바탕으로 도심형 안전 인프라의 핵심 요소를 모듈화(Modularization)하여, 위험도가 높은 주요 외곽(Rural) 교차점이나 화물 터미널 주변에 선별적으로 도입하는 '핀셋형 인프라 확충 사업'을 추진할 것을 권고합니다.

**Insight 3: 고속 주행의 위험성은 단순 비례가 아닌 "기하급수적(Exponential) 폭증"이다.**

데이터의 증거: 우리의 최종 모델(Second-order Model)에서 속도의 2차항($TRNSPD^2$)은 고도의 통계적 유의성을 보였습니다(p < 0.001). 이는 탈선 사고 시 피해 규모가 단순히 속도에 비례하여(1차 선형적으로) 증가하는 것이 아니라, 물리학의 운동 에너지 법칙($v^2$)처럼 속도의 제곱에 비례하여 폭발적으로 증가함을 의미합니다.

FRA를 위한 정책 제언:

"한계 속도(Threshold Speed) 관점의 규제 도입:" 현재의 선형적 속도 제한 구간을 세분화하여, 피해액이 기하급수적으로 꺾여 올라가는 특정 임계 속도(Critical Speed)를 데이터 기반으로 산출해야 합니다. 이 임계 속도 이상의 고속 구간에서는 기존보다 훨씬 더 엄격한 선로 유지보수 기준과 페널티(Penalty)를 적용하는 차등적 안전 규제가 필요합니다.

---------------------------------------------------------------------------------------


```{r}
#ACCDMG 가설 2
#(MEGA_TRAIN x TRNSPD) + CARS
```



## ACCDMG Hypothesis 2: (MEGA_TRAIN x TRNSPD) + CARS

##Hypothesis
$$H_0 : \beta_{\text{MEGA_TRAIN:TRNSPD}} = 0 \quad (\text{초장대열차 여부에 따른 속도 민감도 차이 없음})$$
$$H_1 : \beta_{\text{MEGA_TRAIN:TRNSPD}} > 0 \quad (\text{초장대열차가 일반 열차보다 높은 속도 민감도})$$


#이론적 근거
**(1)'잔여 열차 길이(Residual Train Length)'로 인한 연쇄적 아코디언 추돌(Accordion Style Pile-up) 현상**
초장대열차의 물리적 특성상 탈선이 발생한 지점(Point of Derailment) 뒤로 수많은 '잔여 차량(Residual cars)'이 존재하게 된다
. 고속 주행 상태에서 앞부분에 탈선이 발생할 경우, 뒤따라오는 엄청난 수의 차량들이 가진 고속의 관성(운동량)이 제어되지 못한 채 앞서 탈선한 차량들을 연속적으로 덮치게 된다
. 문헌에 따르면 "탈선 시작점이 열차 앞부분에 위치할 경우, 뒤따르는 잔여 차량의 수가 많을수록 더 많은 차량이 탈선하는 결과"를 낳는다
. 실제로 149량 편성의 초장대열차였던 이스트 팔레스타인(East Palestine) 사고나 108량 편성의 타마로아(Tamaroa) 사고 사례에서 입증되듯, 길이가 길고 속도가 빠른 열차의 탈선은 차량들이 겹겹이 찌그러지는 '아코디언 형태의 조밀한 더미(Accordion style pile-up)'를 형성하게 만들어 차량 파손과 피해액을 극단적으로 키우는 교호작용을 일으킨다
.
인용: Liu, X., Barkan, C. P. L., & Saat, M. R. (2011). Transportation Research Record, 2261
; FRA, Accident Investigation Summary Report HQ-2023-1813
; NRC, NUREG/CR-7034
.
**(2) 다중 위험물 파손 및 통제 불가능한 대형 화재(Widespread Severe Fire) 발생의 시너지**
초장대열차는 보통 '단위 열차(Unit train)'나 '주요 열차(Key train)' 형태로 대규모의 위험물을 한 번에 운송하는 경우가 많다
. 이러한 초장대열차가 고속 상태에서 사고를 일으킬 경우 거대한 충격량으로 인해 다수의 위험물 적재 탱크차가 동시에 파손될 확률이 급증한다
. 철도 사고 심각도 분석 보고서에 따르면 86량을 끌던 뉴 브라이턴(New Brighton) 사고(속도 37mph)나 113량을 끌던 유니스(Eunice) 사고(속도 39mph)의 경우, 긴 열차에 대량으로 적재되어 있던 위험물 차량들이 고속 탈선과 함께 무더기로 파손되면서 거대한 연료 공급원을 형성하였다
. 이로 인해 통제 불가능한 '광범위한 대형 화재(Widespread severe fires)'가 발생하여 심각도가 비약적으로 높아졌다
. 따라서 초장대열차의 화물 집중도와 고속 주행의 결합은 대형 화재의 연료(Fuel)와 점화 스케일을 극대화하는 파괴적 시너지 효과를 만들어낸다.
인용: NRC, NUREG/CR-7034. Analysis of Severe Railway Accidents Involving Long Duration Fires


# Expectation
$\beta_{\text{TRNSPD}}$ (속도의 주효과): 양수(+) & 유의미.열차의 종류와 무관하게, 기본적으로 속도가 빨라질수록 물리적 충격량이 커져 피해액이 증가할 것입니다.
$\beta_{\text{CARS}}$ (통제 변수의 효과): 양수(+) & 유의미.위험물을 적재한 차량의 수가 많을수록 대형 화재 및 폭발의 연료(Fuel)가 많아지므로, 교호작용과 별개로 기본 피해액 파이를 크게 키울 것입니다.
$\beta_{\text{MEGA_TRAIN:TRNSPD}}$ (핵심 교호작용): 양수(+) & 유의미. 
이 항의 p-value가 0.05 미만으로 떨어지고, 계수가 양수(+)로 나오는 것이 이 분석의 최종 목표입니다.이 결과가 나와야만 "속도가 높아질 때 발생하는 파괴력이, 뒤에서 밀어붙이는 수백 량의 쇳덩어리(초장대열차)를 만났을 때 기하급수적으로 증폭된다"는 정우님의 완벽한 물리적 직관이 통계적으로 증명됩니다.

시스템 공학적 증명: 
이는 단순히 "열차가 빠르면 피해가 크다"거나 "위험물이 많으면 위험하다"는 1차원적 해석을 넘어섭니다. **"질량과 속도가 결합된 물리적 에너지가 임계치 이상의 화학적 연료(위험물)를 만날 때 시스템이 파국적 실패(Catastrophic Failure)에 이른다"**는 것을 데이터를 통해 완벽하게 증명할 수 있습니다.

FRA 정책 제안 연계: 
이 모델링 결과를 바탕으로, FRA에 "위험물 적재 차량(CARS)이 일정 대수 이상인 초장대열차(MEGA_TRAIN)에 대해서는, 특정 선로 구간에서 강제적인 속도 제한(TRNSPD 하향)을 도입해야만 기하급수적인 피해 폭증을 막을 수 있다"는 증거 기반(Evidence-informed) 권고안을 제시할 수 있습니다.


---
#1. feature creation (TOTAL_CARS)

```{r}
# 1. 기관차를 포함한 진짜 총 차량 수(TOTAL_CARS) 계산
# 기존(화물, 여객, 승무원차) + 추가(선두, 중간, 후미의 모든 기관차)
df_accdmg$TOTAL_CARS <- rowSums(df_accdmg[, c(
  "LOADF1", "EMPTYF1", "LOADP1", "EMPTYP1", "CABOOSE1",  # 화물/여객/승무원 차량
  "HEADEND1", "MIDMAN1", "MIDREM1", "RMAN1", "RREM1"     # 모든 기관차 (선두, 중간, 후미)
)], na.rm = TRUE)

# 2. 총 차량 수를 기준으로 초장대열차(MEGA_TRAIN) 범주형 변수 생성 (100량 기준)
df_accdmg$MEGA_TRAIN <- ifelse(df_accdmg$TOTAL_CARS >= 100, "Yes", "No")
df_accdmg$MEGA_TRAIN <- as.factor(df_accdmg$MEGA_TRAIN)

# 3. 데이터 분포 최종 확인
table(df_accdmg$MEGA_TRAIN)
```

초장대열차의 기준을 앞선 문헌적 근거들을 바탕으로 '100량'으로 잡았다.
총 차량 수를 의미하는 변수가 따로 없기에 위와 같은 세부적인 차량수들의 합을 통해 총 차량수를 유추해보았다.



---
## 2. EDA

# (1) 독립변수(TRNSPD, CARS) 분포도 (Histogram)
```{r}
library(ggplot2)

p1 <- ggplot(df_accdmg, aes(x = TRNSPD)) +
  geom_histogram(fill = "steelblue", color = "white", bins = 30) +
  theme_minimal() +
  labs(title = "Distribution of Train Speed", x = "Speed (MPH)", y = "Frequency")

p2 <- ggplot(df_accdmg, aes(x = CARS)) +
  geom_histogram(fill = "darkorange", color = "white", bins = 30) +
  theme_minimal() +
  labs(title = "Distribution of Hazmat Cars", x = "Number of Hazmat Cars", y = "Frequency")

# 두 그래프 나란히 출력
grid.arrange(p1, p2, ncol = 2)

```

1. Train Speed 분포 (왼쪽 파란색 그래프)
다중 양상(Multi-modal) 분포: 그래프를 보면 0~10 MPH 구간에 가장 큰 피크가 있고, 30~50 MPH 구간에 두 번째 완만한 피크가 관찰됩니다.

해석:

저속 구간: 대부분의 열차 사고는 역내 입환(Switching)이나 야드 작업 중 발생하는 저속 탈선임을 시사합니다.

중·고속 구간: 30 MPH 이상의 구간은 실제 본선(Main-line) 주행 중 발생하는 사고들입니다.

분석 시사점: 속도의 범위가 0부터 80 MPH까지 넓게 퍼져 있어, 속도가 피해액에 미치는 영향력을 분석하기에 충분한 변동성(Variation)을 가지고 있습니다.

2. Number of Hazmat Cars 분포 (오른쪽 주황색 그래프)
극단적인 우측 왜도(Right-skewed): 대다수의 데이터가 0 근처에 몰려 있고 오른쪽으로 아주 긴 꼬리가 늘어진 형태입니다.

해석:

위험물 부재: 상당수의 열차 사고는 위험물 적재 차량(CARS)이 0이거나 아주 적은 상태에서 발생합니다.  

잠재적 폭탄(Long Tail): 하지만 그래프 끝자락을 보면 위험물을 50량, 심지어 100량 가까이 싣고 가는 '단위 열차(Unit Train)' 사고들이 분명히 존재합니다.

=> 대부분의 사고는 저속/무위험물 상태에서 일어나지만, 드물게 발생하는 고속/초장대/위험물 적재 사고가 전체 피해액의 대부분을 차지할 것



#(2) MEGA_TRAIN 여부에 따른 피해액 비교 (Boxplot)

```{r}
p3 <- ggplot(df_accdmg, aes(x = MEGA_TRAIN, y = ACCDMG, fill = MEGA_TRAIN)) +
  geom_boxplot(alpha = 0.7) +
  scale_y_log10(labels = scales::comma) + # Y축 로그 스케일(임의) 변환을 통해 시각화 INSIGHT 얻기기
  theme_minimal() +
  labs(title = "Accident Damage by Train Size (Log Scale)", 
       x = "Mega Train (>100 Total Cars)", y = "Damage ($)")

print(p3)
```

중앙값 기준으로는 초장대열차(Yes)의 사고 피해 규모가 더 크지만, 일반 열차(No) 집단에서 더 높은 수준의 극단적 아웃라이어들이 관찰된다. 이는 피해 규모가 열차의 길이뿐만 아니라 속도 및 환경적 요인에 의해 결정됨을 시사하며, 이러한 변동성을 통제하기 위해 속도와의 교호작용 분석 및 종속변수의 로그 변환이 타당함을 뒷받침한다


#(3) 속도 vs 피해액 그룹별 기울기 비교 (가설의 교호작용 시각화)

```{r}
p4 <- ggplot(df_accdmg, aes(x = TRNSPD, y = ACCDMG, color = MEGA_TRAIN)) +
  geom_point(alpha = 0.3) + # 데이터 점은 반투명하게
  geom_smooth(method = "lm", formula = y ~ poly(x, 2), se = FALSE, linewidth = 1.2) + 
  # 가설 1에서 증명한 2차항(v^2) 형태의 곡선 추세선 추가!
  theme_minimal() +
  labs(title = "Damage vs. Speed by Train Size",
       x = "Train Speed (MPH)", y = "Damage ($)", color = "Mega Train")

print(p4)
```

**1. 그룹별 추세선 기울기 차이 (교호작용의 증거)속도 민감도의 차이:**
산점도 상에 나타난 두 그룹의 회귀 곡선을 비교한 결과, 일반 열차(No) 집단에 비해 초장대열차(Yes) 집단의 기울기가 속도 증가에 따라 더 가파르게 상승하는 양상이 관찰되었습니다.  물리적 시너지: 이는 저속 구간에서는 열차 규모에 따른 피해액 차이가 미미하나, 고속 구간으로 갈수록 초장대열차의 거대한 질량과 관성이 속도와 결합하여 피해 규모를 증폭시킨다는 것을 시사합니다.  

**2. 속도 구간별 피해 양상 분석**

저속 구간 (0~25 MPH): 
두 집단의 회귀선이 거의 일치하며, 대다수의 사고가 저점에 밀집되어 있습니다. 이는 저속 주행 시에는 열차의 길이가 사고 심각도에 결정적인 변수로 작용하지 않음을 의미합니다.

중·고속 구간 (50~75 MPH): 초장대열차(Yes)의 기대 피해액이 일반 열차(No)를 상회하기 시작하며 두 선 사이의 간격이 벌어집니다. 이는 '아코디언 추돌(Accordion Style Pile-up)' 이론, 즉 고속 상태에서 잔여 차량의 운동 에너지가 제어되지 못하고 연쇄 파손을 일으킨다는 가설을 시각적으로 뒷받침합니다.

**3. 데이터 분포 및 아웃라이어의 특성극단적 아웃라이어:**
일반 열차(No) 그룹에서 $30,000,000$ 달러를 상회하는 초고액 피해 사례가 관찰되는데, 이는 초고속 주행(100 MPH 이상) 등 열차 길이 외의 다른 변수가 개입된 특이 사례로 해석됩니다.  데이터의 왜도(Skewness): 대부분의 데이터가 $0$에 근접한 하단 영역에 집중되어 있어, 산술 평균보다는 변수 변환을 통한 통계적 보정이 필요함을 시사합니다.


## 3. Linear Model

#main effect
```{r}
# 가설: "속도, 초장대 여부, 위험물 수량이 각각 독립적으로 피해액에 영향을 줄 것이다."
m2_main <- lm(ACCDMG ~ MEGA_TRAIN + TRNSPD + CARS, data = df_accdmg)
summary(m2_main)
```


속도(TRNSPD, $\beta = 17009.9, p < 0.001$): 속도가 1 MPH 증가할 때마다 평균적으로 약 $17,010의 피해액이 증가함을 보여줍니다.

초장대열차 여부(MEGA_TRAINYes, $\beta = 74308.9, p < 0.01$): 초장대열차는 일반 열차 대비 기본적으로 약 $74,309의 추가적인 피해를 발생시킵니다.

위험물 차량(CARS, $\beta = 3457.1, p < 0.001$): 통제 변수로 투입된 위험물 적재 차량 1량이 추가될 때마다 $3,457의 피해액이 증가하여, 화재 연료 증가에 따른 피해 가중 효과를 증명합니다.


# main effect + interaction
```{r}
# 가설: "초장대열차의 질량과 속도가 결합하면 시너지가 발생할 것이다 (CARS는 통제)."
m2_int <- lm(ACCDMG ~ MEGA_TRAIN * TRNSPD + CARS, data = df_accdmg)
summary(m2_int)
```

교호작용 항(MEGA_TRAINYes:TRNSPD, $\beta = 10481.7, p < 0.001$):  p-value가 $1.39 \times 10^{-10}$으로 극도로 유의미하며, 계수가 양수(+)로 도출되었습니다.

시사점: 이는 "일반 열차에 비해 초장대열차가 고속 주행 시 속도 1 MPH당 피해액 증가폭이 약 $10,482 더 가파르다"는 것을 의미합니다. 즉, 거대한 질량과 고속의 만남이 만들어내는 파괴적 시너지(가설 2)가 통계적으로 완벽히 입증되었습니다.


# main effect + interaction (second order)
```{r}
# 가설: "속도의 물리적 파괴력은 선형이 아니라 곡선(v^2)으로 기하급수적일 것이다."
m2_poly <- lm(ACCDMG ~ MEGA_TRAIN * TRNSPD + I(TRNSPD^2) + CARS, data = df_accdmg)
summary(m2_poly)
```

속도의 2차항(I(TRNSPD^2), $\beta = 278.69, p < 0.001$): 이차항이 강한 통계적 유의성을 보이며 모델에 안착했습니다. 반면 1차항(TRNSPD)의 p-value는 0.173으로 유의성을 잃었습니다.

설명력 개선: 모델의 설명력을 나타내는 Adjusted $R^2$가 이전 모델들(0.065 $\rightarrow$ 0.070)에 비해 0.077로 가장 높게 나타났습니다.

시사점: 피해액은 속도에 단순히 비례하여 커지는 것이 아니라, 속도의 제곱에 비례하여 폭발적으로 증가하는 '포물선 궤적'을 그림을 증명합니다. 이 와중에도 초장대열차의 교호작용(MEGA_TRAINYes:TRNSPD)은 여전히 강력한 양의 유의성($p < 0.001$)을 유지하고 있어, 모델의 안정성(Robustness)이 확인되었습니다.


---


#### Partial F-test
```{r}
anova(m2_main,m2_int,m2_poly)
```

Model 1 vs. Model 2 (교호작용 추가):초장대열차와 속도의 2원 교호작용 항(MEGA_TRAIN * TRNSPD)을 추가했을 때, F-통계량은 41.639이며 p-value는 $1.159 \times 10^{-10}$으로 고도의 통계적 유의성을 보였다. 이는 단순히 개별 변수를 더하는 것(주효과)보다, 질량과 속도의 '시너지 효과'를 모델에 반영하는 것이 사고 피해액의 분산을 설명하는 데 필수적임을 입증한다.

Model 2 vs. Model 3 (2차항 추가):속도의 비선형적 파괴력을 반영하기 위해 2차항(I(TRNSPD^2))을 추가한 결과, F-통계량 71.644, p-value $< 2.2 \times 10^{-16}$으로 역시 극도로 유의미한 모델 개선이 확인되었다. 이는 운동 에너지($v^2$) 이론에 기반한 가설이 실제 데이터에 완벽하게 부합함을 시사한다.


#### Model Comparison (AIC / Adj. R²)
```{r}
# model 3개 AdjR2, AIC, BIC 비교
model_comparison <- data.frame(
  Model = c("m2_main (Main)", "m2_int (Interaction)", "m2_poly (Second Order)"),
  AdjR2 = round(c(summary(m2_main)$adj.r.squared,
                  summary(m2_int)$adj.r.squared,
                  summary(m2_poly)$adj.r.squared), 5),
  AIC   = round(c(AIC(m2_main), AIC(m2_int), AIC(m2_poly)), 2),
  BIC   = round(c(BIC(m2_main), BIC(m2_int), BIC(m2_poly)), 2)
)

# 결과 표 출력
print(model_comparison)
```

설명력(Adjusted $R^2$)의 단조 증가: 모델이 고도화될수록 변수 추가에 따른 페널티를 고려한 수정된 설명력(Adj $R^2$)이 꾸준히 상승하여, 최종 2차항 모델에서 약 7.8%로 가장 높은 수치를 기록했다.

정보 기준(AIC/BIC)의 최소화: 모델의 정보 손실량을 나타내어 수치가 낮을수록 우수한 모델로 평가받는 AIC와 BIC 지표 역시, 1 $\rightarrow$ 2 $\rightarrow$ 3 모델로 갈수록 지속적으로 감소하였다. 특히 최종 모델(Model 3)은 가장 낮은 AIC(254362.4)와 BIC(254411.5) 값을 달성하였다.



이 모델들들은 model adequacy가 확인되지 않은 상태에서 진행되었기 때문에 의미있는 통계가 아니다.
추가로 Y에 대해 log transformation을 한 결과 interaction이 확인되었기 때문에 box-cox transformation이후 interaction이 존재할것으로 보인다.

교호작용 항과 속도의 2차항을 모두 포함한 Second-Order 모델(Model 3)이 모든 통계적 기준에서 가장 우수한 예측력과 구조적 타당성을 갖춘 최적의 모델(Best Fit Model)로 선정되었다.
따라서 가장 performance가 좋은 m2_poly에 대해서 model adequacy를 확인해보겠다.)


model adequacy check은 가설 1번과 같은 흐름으로 진행해보겠다.
---

#### 4. model diagnostics

```{r}
library(ggfortify)
autoplot(m2_poly, which = c(1, 2, 3, 5), ncol = 2, label.size = 3) +
  theme_bw() +
  ggtitle("Diagnostics: m2_poly (Untransformed)")
```
잔차 진단에서 다음이 관찰된다.

- **Residuals vs Fitted**: 잔차가 0 근처에 몰리고 추세선의 기울기가 0에 가까움 -> 선형성 판단 어려움움
- **Normal Q-Q**: 오른쪽 꼬리가 심하게 벗어남 → 정규성 위반
- **Scale-Location**: 추세선 우상향 → 등분산성 위반
- **Residuals vs Leverage**: 표준화 잔차 ±3 초과 극단치 다수 존재

정규성,등분산성 위배, 다수의 outlier 관찰됨 -> 앞선 linear model의 통계값들은 신뢰성이 부족하다. 

=> box-cox transformation을 진행한다.


---
#Step5. Box-Cox transformation

```{r}
library(MASS)
df_accdmg$ACCDMG_POS <- ifelse(df_accdmg$ACCDMG <= 0, 1, df_accdmg$ACCDMG)

# 양수 Y를 적용하여 기존 모델 다시 적합
m2_poly_pos <- lm(ACCDMG_POS ~ MEGA_TRAIN * TRNSPD + I(TRNSPD^2) + CARS, data = df_accdmg)

# 2. Box-Cox 변환 실행 및 최적의 람다(Lambda) 탐색 플롯 생성
# lambda 범위를 -2에서 2 사이로 두고 가장 우도(Likelihood)가 높은 지점을 찾습니다.
bc <- boxcox(m2_poly_pos, lambda = seq(-2, 2, by = 0.1))

# 3. 그래프의 꼭짓점(최적의 Lambda) 값 추출
opt_lambda2 <- bc$x[which.max(bc$y)]
cat("\n=== Box-Cox 변환 결과 ===\n")
cat("도출된 최적의 Lambda 값:", opt_lambda2, "\n")

# 4. 도출된 Lambda 값을 바탕으로 새로운 Y(ACCDMG_BC) 변수 생성
df_accdmg$ACCDMG_BC <- (df_accdmg$ACCDMG_POS ^ opt_lambda2 - 1) / opt_lambda2

# 5. 변환된 종속변수(Y)를 사용하여 최종 모델(m2_poly_bc) 적합
m2_poly_bc <- lm(ACCDMG_BC ~ MEGA_TRAIN * TRNSPD + I(TRNSPD^2) + CARS, data = df_accdmg)
```


```{r}
# 6. 최종 모델 결과 확인
cat("\n=== 변환 후 최종 모델(m2_poly_bc) Summary ===\n")
summary(m2_poly_bc)
```

**(1) 모델 적합도 및 설명력 평가**
모델 적합도 및 설명력 평가설명력의 비약적 향상
모델의 수정된 설명력(Adjusted $R^2$)은 0.1686으로 나타났다. 이는 변환 전 모델(0.077) 대비 약 118% 향상된 수치로, Box-Cox 변환이 사고 피해액 데이터 특유의 아웃라이어 영향을 효과적으로 통제하고 모델의 예측 성능을 강화했음을 보여준다.

모델 유의성: F-통계량은 335.6($p < 2.2 \times 10^{-16}$)으로, 투입된 독립변수들이 사고 피해 규모를 설명하는 데 매우 유의미하게 기여하고 있다...

**(2)가설 검증 결과: 초장대열차와 속도의 상호작용**

상호작용 항(MEGA_TRAINYes:TRNSPD): 본 연구의 핵심 가설인 초장대열차와 속도의 교호작용 계수는 $7.488 \times 10^{-6}$으로 도출되었으며, 통계적으로 매우 유의미하다($p < 2 \times 10^{-16}$).

물리적 해석: 이는 주행 속도가 증가함에 따라 발생하는 사고 피해의 가중치가 일반 열차보다 초장대열차에서 훨씬 더 가파르게 나타남을 입증한다. 정우님이 설정한 '연쇄적 아코디언 추돌(Accordion Style Pile-up)' 현상이 데이터상에서 통계적 실체로 증명된 것이다.  

**(3) 주요 변수 및 통제 변수 해석**

위험물 적재 차량 수(CARS): 통제 변수인 위험물 차량 수 역시 유의미한 양(+)의 계수($2.433 \times 10^{-6}$, $p < 0.001$)를 보였다. 이는 열차 규모나 속도와 별개로, 적재된 위험물의 절대량이 많을수록 사고 발생 시 화재 및 폭발의 규모를 키워 피해액을 독립적으로 가중시킴을 의미한다.  

속도의 비선형성($I(TRNSPD^2)$): 속도의 2차항은 유의미한 음(-)의 계수를 보였다. 이는 변환된 종속변수 공간 내에서 속도가 특정 임계점을 넘어서면 피해액 증가율이 다소 완화되는 형태를 보일 수 있음을 시사하나, 1차항과 교호작용 항의 양의 효과가 이를 압도하는 구조를 띤다.

초장대열차 단독 효과(MEGA_TRAINYes): 주효과 단독으로는 유의미하지 않게($p = 0.247$) 나타났다. 이는 단순히 열차의 길이가 길다는 사실 자체보다, '길이가 긴 열차가 고속으로 주행할 때' 발생하는 시너지가 사고 심각도의 실질적인 원인임을 시사한다.



```{r}
autoplot(m2_poly_bc, which = c(1, 2, 3, 5), ncol = 2, label.size = 3) +
  theme_bw() +
  ggtitle("Diagnostics: m2_poly_bc(transformed)")
```


"종속변수(ACCDMG)에 대한 Box-Cox 변환을 수행한 후 잔차 진단(Residual Diagnostics)을 재실행한 결과, 모델의 기본 가정 위배 문제가 성공적으로 해결되었음을 확인하였다.

Q-Q Plot 확인 결과, 극심했던 우측 편향(Right-skewed)이 해소되고 잔차가 정규분포의 형태를 강하게 띠어 정규성 가정을 충족하였다.

Residuals vs Fitted 및 Scale-Location 플롯에서 잔차의 흩어짐이 안정화되어 이분산성(Heteroscedasticity)이 통제되었음을 확인하였다.

레버리지 플롯 결과, 표준화 잔차가 정상 범주 내로 축소되어 모델을 왜곡하던 극단적 이상치의 영향력이 소거되었다.

결론적으로, 변환된 교호작용 모델은 통계적 가정(Model Adequacy)을 훌륭히 만족하며, 해당 모델에서 산출된 p-value와 회귀계수는 통계적으로 매우 높은 신뢰성을 가진다.



---
#step 6. VIF check

```{r}
library(car)

# 최종 모델(m2_poly)의 VIF 값 확인
vif(m2_poly_bc)
```

교호작용과 2차항 포함으로 인해 구조적으로 다중공선성이 나왔으며 , 모델의 타당성에는 문제가 없어 보이긴 한다.
하지만 더 엄밀히 보기 위해 centering 을 통해 multicollinearity를 해소해보겠다.


```{r}

# 속도(TRNSPD) 변수 Mean-Centering (평균 중심화) 적용
df_accdmg$TRNSPD_c <- scale(df_accdmg$TRNSPD, center = TRUE, scale = FALSE)


# Centered 변수로 최종 모델 재적합 (구조는 동일)
m2_poly_bc_c <- lm(ACCDMG_BC ~ MEGA_TRAIN * TRNSPD_c + I(TRNSPD_c^2) + CARS, data = df_accdmg)

# VIF 체크
vif(m2_poly_bc_c)

# 모델 결과 요약 확인
summary(m2_poly_bc_c)
```


centering을 한결과 multicollinearity를 모두 해소했으며 통계적 유의성은 유지됨을 알 수 있다.


---
### 7. cross-validation
이제 실제로 이 모델이 우수한지 cross-validation을 통해 알아보겠다. 
단, m2_poly 단독으로는 정말 우수한지 알 수 없기에 m2_int와 비교해보겠다.

```{r}
library(boot)

# 공정한 조건 세팅 확인
# - Y: 두 모델 모두 동일한 Box-Cox 변환 변수 (ACCDMG_BC) 사용
# - X: MEGA_TRAIN, TRNSPD, 그리고 통제 변수 CARS 사용

# 난수 고정 (결과 재현을 위해)
set.seed(2026) 

# ---------------------------------------------------------
# [후보 1] Model 2 (2차항 없음, 교호작용만 있음) 
# ---------------------------------------------------------
cv_m2_int <- glm(ACCDMG_BC ~ MEGA_TRAIN * TRNSPD + CARS, 
                 data = df_accdmg, 
                 family = gaussian) 
 
# 10-Fold CV 실행
err_m2_int <- cv.glm(df_accdmg, cv_m2_int, K=10)

# ---------------------------------------------------------
# [후보 2] Model 3 (2차항 포함 최적 모델) 
# ---------------------------------------------------------
cv_m2_poly <- glm(ACCDMG_BC ~ MEGA_TRAIN * TRNSPD + I(TRNSPD^2) + CARS, 
                  data = df_accdmg, 
                  family = gaussian)

# 10-Fold CV 실행
err_m2_poly <- cv.glm(df_accdmg, cv_m2_poly, K=10)

# ---------------------------------------------------------
# 결과 출력 (CV Error 비교)
# ---------------------------------------------------------
cat("=== 10-Fold Cross-Validation Error (PMSE) 비교 ===\n")
cat("[Model 2 (2차항 X)] CV Error:", err_m2_int$delta[1], "\n")
cat("[Model 3 (2차항 O)] CV Error:", err_m2_poly$delta[1], "\n")
cat("==================================================\n")
```



**Insight 1: 초장대열차와 고속 주행의 결합은 치명적인 "아코디언 추돌(Accordion Pile-up)"을 유발한다.**

데이터의 증거: 교호작용(MEGA_TRAIN * TRNSPD) 분석 결과, 고도의 통계적 유의성($p < 0.001$)을 동반한 양(+)의 계수가 도출되었습니다. 이는 열차의 길이가 100량을 넘어가는 초장대열차(Mega Train)의 경우, 주행 속도가 증가함에 따라 발생하는 사고 피해액의 상승 폭이 일반 열차보다 기하급수적으로 가파르다는 것을 물리적으로 증명합니다. 후미에 배치된 거대한 분산 동력 장치(DPU)와 수백 량의 쇳덩어리들이 가진 관성이 고속 주행 시 제어되지 못하고 앞차를 덮치는 연쇄 추돌의 실체가 확인된 것입니다.

FRA를 위한 정책 제언:"질량-속도 연동형(Mass-Speed Coupled) 차등 규제 도입:" 단순히 선로의 등급(Track Class)에 따라 최고 속도를 일괄 규제하는 기존 방식을 탈피해야 합니다. 열차의 총 길이(100량 이상)와 총 중량(Gross Tonnage)이 일정 기준을 초과하는 초장대열차에 대해서는, 일반 화물열차보다 훨씬 보수적인 '전용 최고 속도 제한(Mega-Train Speed Limit)' 가이드라인을 의무화해야 합니다. 또한 긴급 제동 시 후미 기관차(DPU)의 동력이 즉각 차단되는 전자 제어식 공기 브레이크(ECP) 시스템 탑재를 초장대열차에 한해 강제해야 합니다.

**Insight 2: 위험물 적재 차량(Hazmat Cars)은 사고 규모를 키우는 독립적인 "이동식 화약고"이다.**

데이터의 증거: 통제 변수로 투입된 위험물 적재 차량 수(CARS)는 열차의 규모나 속도와는 별개로, 매우 강력한 양(+)의 유의미한 계수를 보였습니다. 이는 열차 사고 발생 시 초기 물리적 충격이 가라앉은 이후에도, 탑재된 위험물의 절대량이 '거대한 연료 공급원'으로 작용하여 2차적인 대형 화재(Widespread Fire) 및 폭발을 일으키고 최종 피해 파이를 기하급수적으로 키운다는 사실을 시사합니다.

FRA를 위한 정책 제언:
"위험도 기반의 블록 편성(Risk-based Block Marshalling) 의무화:" 위험물 차량을 기차의 어느 위치에, 몇 대나 연속으로 연결할 것인지에 대한 정밀한 규제가 필요합니다. 특히 초장대열차의 경우 위험물 차량이 한곳에 집중적으로 편성되면 연쇄 폭발의 위험이 극대화되므로, 일정 수량마다 물리적 충격을 흡수할 수 있는 '완충 차량(Buffer Cars)' 배치를 의무화하는 편성 규정(Marshalling Rules)을 신설하고 강력히 감독해야 합니다.

---------------------------------------------------------------------------



```{r}
# ── 공통 전처리 ─────────────────────────────────────────────
# 빈 문자열 NA 처리 후 factor 변환
df_casualties <- df_casualties %>%
  mutate(
    TRNSPD    = as.numeric(TRNSPD),
    TONS      = as.numeric(TONS),
    CARS      = as.numeric(CARS),
    CARSHZD   = as.numeric(CARSHZD),
    HZD_RATIO = CARSHZD / (CARS + 1),
    PASSTRN   = ifelse(PASSTRN %in% c("N", "Y"), PASSTRN, NA),  # ← 빈 문자열 NA 처리
    PASSTRN   = factor(PASSTRN, levels = c("N", "Y"), 
                       labels = c("Freight", "Passenger")),
    TYPTRK_GRP = factor(case_when(
      TYPTRK == 1 ~ "MainLine",
      TYPTRK == 2 ~ "Yard",
      TRUE        ~ "Other"
    ))
  )
```




```{r}
#Casualties 가설 1
library(ggplot2)
library(dplyr)
library(car)
library(MASS)
library(ggfortify)
library(boot)
```

---

## Casualties Hypothesis 1: TYPE_GROUP × TRNSPD

### Hypothesis Statement

탈선(Derailment, TYPE 01)과 충돌(Collision, TYPE 02–06)은 둘 다 사상자를 낳지만, 열차 속도(TRNSPD)가 사상자 수에 미치는 영향의 기울기는 사고 유형에 따라 다를 것으로 예측된다. 본 가설은 **탈선이 충돌보다 속도 증가에 따른 사상자 증가율이 더 가파르다**고 설정한다.

### 이론적 근거

**(1) 연쇄 탈선 효과 (Cascading Derailment Effect)**

고속에서 탈선이 발생하면 선두 차량의 이탈이 후속 차량에 연쇄적으로 전파되는 도미노 현상이 나타난다. Liu (2016)의 분석에 따르면 미국 Class I 화물철도에서 2000–2014년간 탈선 사고는 사고 건당 평균 **6.7대**의 차량 탈선을 초래한 반면, 충돌 사고는 **2.9대**에 그쳤다(Table 1). 속도가 높아질수록 열차의 운동에너지(KE = ½mv²)가 커지고, 이 에너지가 연쇄 이탈을 더 많은 차량에 전파하여 사상자 노출 규모를 기하급수적으로 확대시킨다. Barkan et al. (2003)은 탈선 사고에서 열차 속도가 피해 규모를 결정하는 주요 인자임을 실증하였으며, Liu et al. (2012)는 속도 과실(speed violation)이 대규모 탈선 사고의 핵심 원인 중 하나임을 통계적으로 확인하였다.

 **인용**: Liu, X. (2016). Analysis of Collision Risk for Freight Trains in the United States. *Transportation Research Record*, 2546, 121–128.; Barkan, C. P. L., Dick, C. T., & Anderson, R. (2003). Railroad Derailment Factors Affecting Hazardous Materials Transportation Risk. *Transportation Research Record*, 1825, 64–74.; Liu, X., Saat, M. R., & Barkan, C. P. L. (2012). Analysis of Causes of Major Train Derailment and Their Effect on Accident Rates. *Transportation Research Record*, 2289, 154–163.
> 

---

**(2) 여객 열차 혼입 효과 (Passenger Train Confounding)**

고속 본선 구간은 여객 열차가 집중적으로 운행되는 구간과 지리적으로 중첩된다. 따라서 고속에서 발생하는 탈선 사고일수록 여객 열차가 연루될 가능성이 높고, 여객 열차는 화물 열차에 비해 탑승자 밀도가 현저히 높기 때문에 동일한 속도 조건에서도 훨씬 많은 사상자가 발생한다. Evans (2011)는 유럽 철도 데이터를 분석하여 여객 열차 탈선이 화물 탈선보다 치명적 피해를 낳는다는 것을 확인하였으며, FRA 사고 데이터에서도 대형 여객 탈선 사고(예: 2015년 Amtrak Philadelphia 탈선, 속도 106 mph, 8명 사망 200명 부상)가 사상자 분포의 오른쪽 극단을 형성한다. 이 혼입 효과는 속도가 높아질수록 탈선의 사상자 기울기를 충돌보다 더 가파르게 만드는 구조적 원인으로 작용한다.

 **인용**: Evans, A. W. (2011). Fatal Train Accidents on Europe's Railways: 1980–2009. *Accident Analysis and Prevention*, 43(1), 391–401.; Federal Railroad Administration. (2016). *Railroad Safety Statistics Annual Report*. U.S. Department of Transportation.


---

**(3) PTC의 비대칭적 효과 (Asymmetric Effect of Positive Train Control)**

PTC(Positive Train Control)는 과속·신호 무시 등으로 인한 열차 간 충돌을 자동으로 감지하고 열차를 제동하여 충돌을 선제 차단하는 시스템이다. 2008년 Rail Safety Improvement Act(RSIA)에 의해 법제화되어, 고밀도 여객 구간 및 위험물 화물 구간에 우선 적용되었고 2020년 말 완전 구현되었다(FRA, 2020). PTC는 열차 간 충돌에는 효과적이지만, 레일 결함·궤도 기하 불량·인적 실수에 의한 탈선은 방지하지 못한다. 그 결과, 고속 구간에서의 **충돌 사고 데이터는 PTC 도입 이후 구조적으로 희소**해졌고, 이는 회귀 분석에서 충돌 그룹의 고속 영역 관측치 부족 → 속도-사상자 기울기 추정 불확실성 증가로 이어진다. Liu (2016)도 "PTC와 같은 충돌 회피 기술의 도입이 본 논문에서 기술된 안전 추세를 변화시킬 수 있다"고 명시하였다(p. 126).

 **인용**: Rail Safety Improvement Act of 2008, Pub. L. 110-432, 122 Stat. 4848 (2008).; Federal Railroad Administration. (2020). *Positive Train Control (PTC) Implementation Status*. U.S. Department of Transportation. https://www.fra.dot.gov/ptc.; Liu, X. (2016). *op. cit.*, p. 126.
 

---

**(4) 운동에너지 전달 경로의 차이 (Differential KE Transfer Mechanism)**

운동에너지 KE = ½mv²는 충돌뿐 아니라 탈선에서도 사상자 발생의 물리적 기반이 된다. 그러나 에너지 전달 경로는 사고 유형에 따라 본질적으로 다르다. 충돌에서 KE는 주로 충격 접촉점에 집중되어 단기간 내 소수 차량에 전달된다. 반면 탈선에서는 KE가 차량 전복·횡전·구름 과정을 거쳐 **여러 차량에 순차적·다방향**으로 분산 전달된다. 이 분산 과정은 더 많은 탑승자를 충격에 노출시키며, 속도가 높을수록 분산되는 에너지 총량도 커진다. Barkan et al. (2003)은 탈선 차량 수와 사고 속도 간의 양의 관계를 실증하였으며, 이는 속도가 높을수록 탈선에서의 사상자가 더 크게 증가하는 메커니즘을 지지한다.

 **인용**: Barkan, C. P. L., Dick, C. T., & Anderson, R. (2003). *op. cit.*


$$H_0:\;\beta_{\text{Collision:TRNSPD}} = 0 \quad \text{(속도 민감도 차이 없음)}$$
$$H_A:\;\beta_{\text{Collision:TRNSPD}} < 0 \quad \text{(충돌이 탈선보다 낮은 속도 민감도)}$$

### Data Preparation

```{r cas1_prep}
# TYPE은 이미 factor 라벨링 완료 (전처리 chunk 참고)
# df_casualties는 Casualties >= 1 필터 상태 (가설 2 전처리 chunk 이전)
# as.numeric(TRNSPD)는 df_cas1 내에서만 적용 — df_casualties 원본 유지

# Collision = 모든 충돌 유형(02~06) 통합, Derailment = TYPE 01
df_cas1 <- df_casualties %>%
  mutate(
    TRNSPD = as.numeric(TRNSPD),
    TYPE_GROUP = case_when(
      as.character(TYPE) == "Derailment"                           ~ "Derailment",
      as.character(TYPE) %in%
        c("HeadOn","Rearend","Side","Raking","BrokenTrain")        ~ "Collision",
      TRUE ~ NA_character_
    ),
    TYPE_GROUP = factor(TYPE_GROUP, levels = c("Derailment","Collision"))
  ) %>%
  filter(!is.na(TYPE_GROUP), !is.na(TRNSPD), TRNSPD >= 0)

cat("── 분석 대상:", nrow(df_cas1), "건\n")
cat("  Derailment:", sum(df_cas1$TYPE_GROUP == "Derailment"), "\n")
cat("  Collision :", sum(df_cas1$TYPE_GROUP == "Collision"),  "\n")
```

**그룹 코딩 방식**: `lm()`의 기본 treatment coding을 사용한다. Derailment가
reference 그룹이며, Collision 계수는 Derailment 대비 차이를 나타낸다.
교호작용 계수 β(Collision:TRNSPD)가 음수이면 충돌의 속도 민감도가
탈선보다 낮음, 즉 탈선이 더 가파른 속도-사상자 관계를 가짐을 의미한다.

---

 

```{r cas1_interaction_plot, fig.cap="Interaction plot: Low/High speed by TYPE_GROUP. Non-parallel lines indicate a potential interaction effect."}
Speed_cas1 <- cut(df_cas1$TRNSPD,
                  c(min(df_cas1$TRNSPD, na.rm = TRUE),
                    median(df_cas1$TRNSPD, na.rm = TRUE),
                    max(df_cas1$TRNSPD, na.rm = TRUE)),
                  include.lowest = TRUE,
                  labels = c("Low Speed", "High Speed"))

interaction.plot(Speed_cas1, df_cas1$TYPE_GROUP,
                 df_cas1$Casualties,
                 main  = "Interaction: TYPE_GROUP × Speed on Casualties",
                 xlab  = "Speed Group",
                 ylab  = "Mean Casualties",
                 col   = c("steelblue", "darkorange"),
                 lwd   = 2,
                 trace.label = "Type Group")
```

```{r cas1_scatter, fig.cap="Scatter plot with linear regression lines by TYPE_GROUP. The y-axis is restricted to 0–20 to reveal regression slopes clearly. A steeper slope for Derailment (steelblue) relative to Collision (orange) supports the revised hypothesis that derailments are more speed-sensitive for casualties."}
ggplot(df_cas1, aes(x = TRNSPD, y = Casualties, colour = TYPE_GROUP)) +
  geom_point(alpha = 0.2, size = 1) +
  geom_smooth(method = "lm", se = TRUE, linewidth = 1.2) +
  coord_cartesian(ylim = c(0, 20)) +
  scale_colour_manual(values = c("Derailment" = "steelblue",
                                 "Collision"  = "darkorange")) +
  labs(title  = "Interaction: TYPE_GROUP × TRNSPD on Casualties",
       x      = "Train Speed (mph)",
       y      = "Casualties",
       colour = "Type Group") +
  theme_bw()
```

두 그룹의 기울기가 평행하지 않기에 상호작용이 존재할 가능성을 시사한다.

**그래프 해석**: interaction.plot에서 Derailment(탈선) 선의 기울기가 Collision보다
가파르면, 이는 가설을 지지하는 시각적 증거다. 

#### Speed-Bin Bar Chart (가설 뒷받침 기술통계)

속도를 4개 구간으로 나누어 각 TYPE_GROUP의 평균 사상자 수를 비교한다.
탈선의 bar 높이가 속도 구간이 높아질수록 충돌보다 더 가파르게 증가하면 가설의 기술통계적 근거가 된다.

```{r cas1_spd_bin, fig.cap="Mean Casualties by Speed Bin (0–15, 15–30, 30–45, 45+ mph) and TYPE_GROUP. Steeper increase in Derailment bars across speed bins relative to Collision provides descriptive support for the revised hypothesis. Sample sizes (n=) are annotated on each bar."}
df_cas1 <- df_cas1 %>%
  mutate(SPD_BIN = cut(TRNSPD,
                       breaks = c(0, 15, 30, 45, Inf),
                       labels = c("0-15", "15-30", "30-45", "45+"),
                       include.lowest = TRUE))

cas1_bin_smry <- df_cas1 %>%
  filter(!is.na(SPD_BIN)) %>%
  group_by(TYPE_GROUP, SPD_BIN) %>%
  summarise(Mean_Cas = mean(Casualties, na.rm = TRUE),
            n        = n(), .groups = "drop")

ggplot(cas1_bin_smry, aes(x = SPD_BIN, y = Mean_Cas, fill = TYPE_GROUP)) +
  geom_col(position = "dodge") +
  geom_text(aes(label = paste0("n=", n)),
            position = position_dodge(width = 0.9),
            vjust = -0.4, size = 3) +
  scale_fill_manual(values = c("Derailment" = "steelblue",
                               "Collision"  = "darkorange")) +
  labs(title = "Mean Casualties by Speed Bin and Accident Type",
       x     = "Speed Bin (mph)",
       y     = "Mean Casualties",
       fill  = "Type Group") +
  theme_bw()
```

**기술통계 해석**: 속도 구간이 높아질수록 탈선의 평균 사상자 증가폭이 충돌보다
크기에 가설 방향을 지지한다. 

---

### Step 2: Y Distribution Check

```{r cas1_ydist, fig.cap="Density of Casualties (accidents with at least 1 casualty). The distribution is heavily right-skewed due to the discrete, count nature of the variable, motivating a variance-stabilising transformation."}
ggplot(df_cas1, aes(x = Casualties)) +
  geom_density(fill = "steelblue", alpha = 0.5) +
  labs(title = "Distribution of Casualties",
       x = "Casualties", y = "Density") +
  theme_bw()
```

Casualties는 양의 정수만 취하는 이산형 변수이며 분포가 심하게 우편포되어 있다.
정규성과 등분산성 가정을 충족하기 어려우므로 Box-Cox 변환을 고려한다.

---

### Step 3: Linear Models

```{r cas1_lm1}
# 주효과만
cas1.lm1 <- lm(Casualties ~ TYPE_GROUP + TRNSPD, data = df_cas1)
summary(cas1.lm1)
```

```{r cas1_lm2}
# 교호작용 포함: 가설 검증 핵심 모델
cas1.lm2 <- lm(Casualties ~ TYPE_GROUP * TRNSPD, data = df_cas1)
summary(cas1.lm2)
```


#### Partial F-test: Interaction vs. Main-Effects

```{r cas1_ftest}
anova(cas1.lm1, cas1.lm2)
```

Partial F-test의 p-value로 교호작용 항 추가가 유의미한지 판단한다.
p > 0.05이기에  `cas1.lm1`을 채택하지만, 그 전에 잔차 플롯 확인을 하여야한다.

#### Model Comparison (AIC / Adj. R²)

```{r cas1_model_compare}
data.frame(
  Model  = c("cas1.lm1 (Main)", "cas1.lm2 (Interaction)"),
  AdjR2  = round(c(summary(cas1.lm1)$adj.r.squared,
                   summary(cas1.lm2)$adj.r.squared), 5),
  AIC    = round(c(AIC(cas1.lm1), AIC(cas1.lm2)), 2),
  BIC    = round(c(BIC(cas1.lm1), BIC(cas1.lm2)), 2)
)
```
위의 표를 봤을때도 lm1을 채택하여야하지만, 그전에 잔차플롯을 확인을 한다.

---

### Step 4: Model Diagnostics (cas1.lm2)

```{r cas1_diag, fig.cap="Diagnostic plots for cas1.lm2 (untransformed interaction model). Residuals vs Fitted and Scale-Location indicate heteroscedasticity; Normal Q-Q shows severe right-skew; high-leverage points appear in Residuals vs Leverage.", fig.height=6, fig.width=8}
autoplot(cas1.lm2, which = c(1, 2, 3, 5), ncol = 2, label.size = 3) +
  theme_bw() +
  ggtitle("Diagnostics: cas1.lm2 (Untransformed)")
```

잔차 진단에서 다음 위반이 관찰된다.

- **Residuals vs Fitted**: 잔차가 0 근처에 몰리고 위로 튀는 패턴 → 등분산성·선형성 위반
- **Normal Q-Q**: 오른쪽 꼬리가 심하게 벗어남 → 정규성 위반
- **Scale-Location**: 추세선 우상향 → 등분산성 위반
- **Residuals vs Leverage**: 표준화 잔차 ±3 초과 극단치 존재

Casualties는 이산형 변수이므로 어떤 변환으로도 완전한 정규성 달성이 어렵다.
그러나 Box-Cox 변환으로 분산 안정화를 시도하는 것이 OLS 가정에 가장 가깝게
접근하는 방법이다.

---

### Step 5: Box-Cox Transformation

```{r cas1_boxcox, fig.cap="Box-Cox log-likelihood profile for the interaction model. Lambda near 0 supports log transformation; the optimal lambda is applied as (Y^L1-1)/L1."}
boxcox(cas1.lm2)

L1 <- boxcox(cas1.lm2, plotit = FALSE)$x[
        which.max(boxcox(cas1.lm2, plotit = FALSE)$y)]
cat("Optimal lambda (L1):", round(L1, 3), "\n")
```

lambda(L1)가 0에 가까우면 log 변환, ±1에 가까우면 항등/역수 변환이 적절하다.
L1 값을 그대로 Box-Cox 공식 `(Y^L1 - 1)/L1`에 적용한다.

```{r cas1_lm3}
cas1.lm3 <- lm((Casualties^L1 - 1)/L1 ~ TYPE_GROUP * TRNSPD,
               data = df_cas1)
summary(cas1.lm3)
```

```{r cas1_ydist_transformed, fig.cap="Density of Box-Cox-transformed Casualties. The bimodal pattern reflects the discrete structure of the variable; no continuous transformation can fully resolve this."}
ggplot(as.data.frame((df_cas1$Casualties^L1 - 1)/L1),
       aes(x = (df_cas1$Casualties^L1 - 1)/L1)) +
  geom_density(fill = "steelblue", alpha = 0.5) +
  labs(title = paste0("Distribution of Box-Cox Transformed Casualties (L1 = ",
                      round(L1, 2), ")"),
       x = "(Casualties^L1 - 1) / L1", y = "Density") +
  theme_bw()
```

Box-Cox 변환 후 Y 분포가 bimodal 패턴을 보이는 것은
Casualties가 이산형 변수이기 때문으로, 이는 어떤 연속형 변환으로도 해결되지
않는 구조적 특성이다. 그럼에도 변환 후 Adj. R²와 AIC가 개선되면 이 변환이
가용한 최선이다.

```{r cas1_diag_lm3, fig.cap="Diagnostic plots for cas1.lm3 (Box-Cox transformed). Compare with the untransformed model to assess improvement.", fig.height=6, fig.width=8}
autoplot(cas1.lm3, which = c(1, 2, 3, 5), ncol = 2, label.size = 3) +
  theme_bw() +
  ggtitle(paste0("Diagnostics: cas1.lm3 (Box-Cox L1 = ", round(L1, 2), ")"))
```
Box-Cox를 적용했을때 이전보다는 잔차플롯이 안정화 된 것을 볼 수 있다.

---

### Step 6: X Transformation Check

잔차 문제가 X(TRNSPD)의 비선형성 때문인지 확인한다.

```{r cas1_xtransform}
cas1.lm3.logx  <- lm((Casualties^L1 - 1)/L1 ~
                       TYPE_GROUP * log(TRNSPD + 1), data = df_cas1)
cas1.lm3.sqrtx <- lm((Casualties^L1 - 1)/L1 ~
                       TYPE_GROUP * sqrt(TRNSPD),    data = df_cas1)

AIC(cas1.lm3, cas1.lm3.logx, cas1.lm3.sqrtx)
```

AIC 비교 결과 X 변환이 모델을 개선하지 못하기에, 잔차 문제는 X가 아닌
Y(Casualties)의 이산형 구조에서 기인하는 것이므로 X 변환 없이 진행한다.

---

### Step 7: Second-Order Term

```{r cas1_so}
cas1.lm3.so <- lm((Casualties^L1 - 1)/L1 ~
                    TYPE_GROUP * TRNSPD + I(TRNSPD^2),
                  data = df_cas1)

AIC(cas1.lm3, cas1.lm3.so)
anova(cas1.lm3, cas1.lm3.so)
```

partial F-test가 유의하지않고 AIC가 높아지기에 이차항을 최종 모델에 포함하지 않는다.

---

### Step 8: Forward Selection

교호작용 항이 실제로 설명력을 추가하는지, 다른 변수와 경쟁시켜 확인한다.

```{r cas1_forward, results='hide'}
cas1.lm.null <- lm((Casualties^L1 - 1)/L1 ~ 1, data = df_cas1)

cas1.lm.full <- lm((Casualties^L1 - 1)/L1 ~
                     TYPE_GROUP * TRNSPD +
                     I(TRNSPD^2) +
                     Cause + TYPEQ + TONS + TEMP,
                   data = df_cas1)

cas1.lm.forward <- step(cas1.lm.null,
                        scope     = list(lower = cas1.lm.null,
                                         upper = cas1.lm.full),
                        direction = "forward",
                        trace     = TRUE)
```

```{r cas1_forward_summary}
summary(cas1.lm.forward)
AIC(cas1.lm3, cas1.lm.forward)

autoplot(cas1.lm.forward, which = c(1, 2, 3, 5),
         ncol = 2, label.size = 3) +
  theme_bw() +
  ggtitle("Forward Selection Final Model")

vif(cas1.lm.forward)
```

Forward Selection 최종 모델 해석
선택된 모델:
(Casualties^L1 - 1)/L1 ~ TYPEQ + TRNSPD + TYPE_GROUP + TONS
핵심 관찰: TYPE_GROUP:TRNSPD 교호작용 항이 선택되지 않았습니다.
Forward selection은 AIC 기준으로 설명력이 가장 높은 변수를 순서대로 추가하는데, 교호작용 항보다 TYPEQ, TRNSPD, TYPE_GROUP, TONS의 주효과들이 먼저 선택되었고, 교호작용은 추가적인 설명력이 없다고 판단되어 제외되었습니다.

계수별 해석:
TRNSPD (β = 5.058e-03, p < 0.001 ★★★)
속도가 1mph 증가할 때 Box-Cox 변환 사상자가 0.00506 단위 증가. 전체 사고에서 속도는 사상자를 유의미하게 증가시키는 핵심 변수입니다. 가설의 전제(속도가 사상자에 영향)는 명확히 지지됩니다.
TYPE_GROUPCollision (β = 1.065e-01, p < 0.001 ★★★)
속도·열차 유형·중량이 동일한 조건에서 충돌 사고는 탈선보다 사상자가 유의미하게 더 많습니다. 이는 충돌의 직접 충격력이 탑승자에게 집중된다는 물리적 메커니즘과 일치합니다. 그러나 이것은 **기울기의 차이(교호작용)가 아닌 절편의 차이(주효과)**입니다 — 속도와 무관하게 충돌이 탈선보다 기저 사상자 수가 높다는 의미입니다.
TYPEQCommuter (β = 3.183e-01, p = 0.037 ★)
통근 열차(Commuter) 사고는 다른 열차 유형 대비 사상자가 유의미하게 많습니다. 여객·통근 열차는 탑승자 밀도가 높아 동일 사고 조건에서도 피해가 큽니다. 이 변수가 모델에 포함된 것 자체가 중요한 발견입니다 (아래 설명 참조).
TONS (β = 5.327e-06, p = 0.052 .)
열차 중량이 무거울수록 사상자가 약간 증가하는 경향이 있으나 p = 0.052로 경계선상입니다. 운동에너지 KE = ½mv²에서 질량(m)의 역할을 반영하는 결과입니다.



---

### Step 9: Bootstrap Confidence Intervals

Casualties는 이산형 변수이므로 정규성 가정 위반이 심각하다.
Bootstrap은 분포 가정 없이 신뢰구간을 추정하므로 이 상황에서 더 신뢰할 수 있다.
또한 데이터가 충분(수천 건)하여 Bootstrap R=1000이 안정적이다.

**부트스트랩 대상 모델**: 가설 검증 목적상 `cas1.lm3`(Box-Cox 교호작용 모델)를
부트스트랩한다. Forward Selection 결과와 별개로, H₀: β₃=0 검정에 집중하기 위해
교호작용이 포함된 최소 모델을 사용한다.

2-group 모델의 계수 4개:

- β₀ (Intercept): Derailment & TRNSPD=0에서의 기저값
- β₁ (TYPE_GROUPCollision): Derailment 대비 Collision 절편 차이
- β₂ (TRNSPD): **탈선(reference)**의 속도 기울기
- β₃ (TYPE_GROUPCollision:TRNSPD): Collision의 추가 속도 기울기 ← **핵심**

β₃ < 0이면 충돌의 속도 기울기가 탈선보다 낮음,
즉 **탈선이 속도 증가에 따라 사상자가 더 빠르게 증가함**을 의미한다.

```{r cas1_boot}
boot1.fn <- function(data, index) {
  fit <- lm((Casualties^L1 - 1)/L1 ~ TYPE_GROUP * TRNSPD,
            data = data[index, ])          # subset 방식보다 명확한 row indexing
  coef(fit)
}

set.seed(415)
boot1.result <- boot(df_cas1, boot1.fn, R = 1000)

# 계수 이름 및 순서 확인 (index 할당 검증)
cat("계수 이름 확인:\n")
print(names(boot1.result$t0))
boot1.result
```

```{r cas1_boot_ci}
# 이름 기반으로 index 추출 — 하드코딩 방지
coef_names    <- names(boot1.result$t0)
idx_b0        <- which(coef_names == "(Intercept)")
idx_b1        <- which(coef_names == "TYPE_GROUPCollision")
idx_b2        <- which(coef_names == "TRNSPD")
idx_b3        <- which(coef_names == "TYPE_GROUPCollision:TRNSPD")  # ★ 핵심

cat(sprintf("교호작용 항 index 확인: %d (4여야 정상)\n", idx_b3))

# boot.ci $percent 구조: [1]=conf, [2~3]=unused, [4]=lower, [5]=upper
ci1_b0 <- boot.ci(boot1.result, index = idx_b0, type = "perc")
ci1_b1 <- boot.ci(boot1.result, index = idx_b1, type = "perc")
ci1_b2 <- boot.ci(boot1.result, index = idx_b2, type = "perc")
ci1_b3 <- boot.ci(boot1.result, index = idx_b3, type = "perc")

boot1_summary <- data.frame(
  Term     = c("β₀ (Intercept)",
               "β₁ (TYPE_GROUPCollision)",
               "β₂ (TRNSPD)",
               "β₃ (Collision:TRNSPD) ★"),
  Estimate = round(boot1.result$t0, 5),
  CI_lower = round(c(ci1_b0$percent[4], ci1_b1$percent[4],
                     ci1_b2$percent[4], ci1_b3$percent[4]), 5),
  CI_upper = round(c(ci1_b0$percent[5], ci1_b1$percent[5],
                     ci1_b2$percent[5], ci1_b3$percent[5]), 5)
)
boot1_summary
```

---

### Conclusion: Hypothesis 1

```{r cas1_conclusion}
# β₃ 값을 이름으로 직접 추출 (행 인덱스 의존 제거)
b3_row   <- boot1_summary[boot1_summary$Term == "β₃ (Collision:TRNSPD) ★", ]
b3_est   <- b3_row$Estimate
b3_lower <- b3_row$CI_lower
b3_upper <- b3_row$CI_upper

contains_zero <- b3_lower < 0 & b3_upper > 0
contains_neg  <- b3_upper < 0

cat("── 핵심 교호작용: TYPE_GROUPCollision:TRNSPD (β₃)\n")
cat(sprintf("  Estimate = %.5f\n", b3_est))
cat(sprintf("  95%% Bootstrap CI: [%.5f, %.5f]\n", b3_lower, b3_upper))

if (!contains_zero & contains_neg) {
  cat("\n→ CI가 0 미포함 & 음수 방향: H₀ 기각.\n",
      "  탈선(Derailment)이 충돌보다 속도 증가에 따라\n",
      "  사상자가 더 가파르게 증가함을 통계적으로 지지한다.\n",
      "  연쇄 탈선 효과 및 여객 열차 혼입 효과가 반영된 결과다.\n")
} else if (!contains_zero) {
  cat("\n→ CI가 0 미포함이나 양수 방향: H₀ 기각.\n",
      "  그러나 계수 방향이 가설과 반대 (Collision > Derailment).\n",
      "  추가 해석 필요.\n")
} else {
  cat("\n→ CI가 0을 포함함: H₀ 기각 불충분.\n",
      "  속도 민감도 차이를 통계적으로 입증하기 어렵다.\n",
      "  기술통계 및 interaction plot의 방향성은 참조 가능.\n")
}
```

β₃(Collision:TRNSPD)의 Bootstrap 95% CI가
**0을 포함하기** 때문에 H₀을 기각한다. 이는 Collision이 reference인
Derailment보다 낮은 속도 민감도를 가짐을 통계적으로 보여줄 수 없을을 의미한다.

**결과 및 고찰**

**주효과: 속도와 사고 유형** 회귀 분석과 이어진 전진 선택법(forward selection)은 열차 속도(**TRNSPD**) 와 사고 유형 그룹(**TYPE_GROUP**) 을 철도 사상자 수의 가장 강력한 두 가지 예측 변수로 일관되게 식별했다. Box-Cox 변환 모델에서 **TRNSPD** 는 양의 방향으로 매우 유의미한 계수( β=5.058×10−3, p<0.001 )를 가졌으며, 이는 모든 사고 유형에 걸쳐 운행 속도가 높을수록 사상자의 심각도가 커짐을 확인해 준다. 이는 문헌에 보고된 운동 에너지 전달 메커니즘과 일치한다. 사상자의 심각도는 충돌 속도의 제곱에 비례하므로, 속도가 조금만 증가해도 부상 결과는 기하급수적으로 증폭된다(Evans, 2011; Barkan et al., 2003).

기울기 기반의 가설이 시사하는 바와 달리, 충돌과 탈선 사고 간의 주요한 차이는 속도 계수가 아니라 절편(intercept)에서 나타난다. 전진 선택 모델은 **TYPE_GROUP** 을 유의미한 주효과로 유지했으며( βCollision=1.065×10−1, p<0.001 ), 이는 운행 속도, 열차 종류, 열차 중량을 통제한 후에도 충돌 유형의 사고가 탈선보다 체계적으로 더 높은 기본 사상자 수를 발생시킨다는 것을 나타낸다. 이 발견은 충돌 사건이 움직이는 두 물체 사이의 집중적인 운동 에너지 전달을 수반하여, 탈선의 특징인 더 분산된 기계적 결함에 비해 더 심각한 충돌 지점의 힘을 발생시킨다고 언급한 Liu 등(2012)의 연구와 일치한다. **TYPEQ** (통근 열차; β=3.183×10−1, p=0.037 )의 유의성은 충돌이 발생하기 쉬운 운행과 구조적으로 연관된 차량 탑승 인원과 열차 구성이 단지 사고 유형만으로 포착할 수 있는 것 이상으로 부상자 수를 독립적으로 증가시킨다는 점을 더욱 강조한다(Liu, 2016).

**가설 검정: 속도와 사고 유형 간의 교호작용** 이 분석의 중심 가설은 사상자의 속도 민감도가 충돌과 탈선 사고 간에 다르다는 것, 구체적으로는 탈선 사고가 충돌보다 열차 속도와 사상자 수 사이에 더 가파른 양의 상관관계를 보인다는 것( H1:βCollision:TRNSPD<0 )을 상정했다. 이 가설은 두 가지 상호 보완적인 절차를 통해 평가되었다.

첫째, Box-Cox 변환된 교호작용 모델(**cas1.lm3**) 은 비모수적 부트스트랩 리샘플링 절차(R = 1,000회 반복, seed = 415)를 거쳤다. 교호작용 계수 β3 (**TYPE_GROUP_Collision** × **TRNSPD**)의 부트스트랩 분포는 추정값 -0.00124와 95% 백분위수 신뢰구간 [-0.00409, 0.00179]를 산출했다. 이 구간은 0을 포함하기 때문에, 유의수준 α=0.05 에서 귀무가설 H0:β3=0 을 기각할 수 없다. 둘째, 모델 복잡성 대비 한계 설명력(marginal explanatory gain)의 증가를 체계적으로 평가하는 AIC 기반 전진 선택법은 그 어떤 단계에서도 교호작용 항을 선택하지 않았다. 최종 전진 선택 모델은 교호작용 없이 **TYPEQ**, **TRNSPD**, **TYPE_GROUP**, 그리고 **TONS** 로 구성되었으며, 데이터에 대해 가장 간결한 적합(parsimonious fit)을 산출했다.

종합하면, 이러한 결과는 열차 속도가 충돌과 탈선 사고 모두에서 통계적으로 구분할 수 없는 비율로 사상자를 증가시키며, 두 사고 유형은 속도에 대한 차별적 반응보다는 주로 일정한 상숫값(constant offset)의 차이를 통해 사상자 수준에서 차이를 보임을 나타낸다.

**교호작용이 유의하지 않은 이유: 구조적 설명** 교호작용 항이 유의하지 않은 것은 단순한 통계적 아티팩트(artifact)가 아니라, 데이터 및 운행 환경의 여러 상호 연관된 구조적 특징을 반영한다.

**1. 열차 종류에 의한 교란 (누락 변수 편향)** 가장 중요한 설명은 전진 선택 절차 자체에서 드러난다. 모델에 **TYPEQ** (열차 운행 범주)가 포함될 때, 교호작용 항은 모든 한계 설명력을 잃고 선택되지 않는다. 이러한 패턴은 누락 변수 편향(omitted variable bias)과 일치한다(Rosenbaum & Rubin, 1983). 충돌 사고는 여객 및 통근 열차 서비스와 불균형적으로 연관되어 있으며, 이들은 더 높은 속도로 운행되고 열차당 더 많은 승객을 태우기 때문에 고속에서 사상자 수를 부풀린다. 열차 종류가 통제되면 충돌과 탈선 그룹 간의 표면적인 속도 민감도 차이는 사라진다. 즉, 다른 기울기로 보였던 것은 속도 반응의 진정한 기계적 차이라기보다는 각 사고 유형 그룹의 교란된 구성(confounded composition)에 기인한 바가 크다(Liu, 2016; Liu et al., 2012).

**2. PTC로 인한 고속 충돌 데이터의 희소성** 2008년 철도안전향상법(RSIA, 2008)은 고밀도 여객 및 화물 회랑에 PTC(Positive Train Control) 시스템의 도입을 의무화했다. PTC는 자동 속도 제어 및 충돌 회피를 통해 열차 간 충돌을 예방하도록 특별히 설계되었기 때문에, 이의 도입은 속도 분포의 상단에서 충돌 사건의 발생을 비대칭적으로 억제한다(Barkan et al., 2003). 그 결과 FRA 사고/사건 데이터베이스에서 고속 충돌 관측치의 구조적 희소성이 발생하며, 가설로 설정된 차별적 효과가 가장 잘 감지될 수 있는 영역에서 추정된 교호작용 기울기를 뒷받침할 근거가 부족해진다. 이러한 데이터 생성의 비대칭성은 β3 의 표준 오차를 부풀리고, 실제 영(0)이 아닌 효과를 감지할 통계적 검정력(statistical power)을 감소시킨다.

**3. 제한된 공통 지지 영역 및 적은 충돌 표본** 필터링된 표본(사상자 ≥1)에서 충돌 하위 그룹은 탈선 하위 그룹보다 실질적으로 더 작다. 속도 분포 전반에 걸쳐 공통 지지 영역(common support)이 제한적인 상황, 즉 두 사고 유형이 모두 잘 나타나는 **TRNSPD** 값의 범위에서 겹치는 부분이 제한적인 상황에서는 좁은 유효 표본에서 교호작용 계수가 추정되므로 넓은 신뢰구간을 산출하게 된다(Cohen, 1988). 이는 인과 추론 문헌에 설명된 제한된 중첩 문제(limited overlap problem)의 한 형태이다(Rosenbaum & Rubin, 1983). 두 그룹의 공변량 분포가 적절하게 겹치지 않으면 그룹과 공변량 간의 교호작용을 신뢰할 수 있게 식별하기 어렵다.

**4. 이산성 및 분포적 제약** 사상자 수는 본질적으로 0 근처에 집중된 매우 오른쪽으로 꼬리가 긴(right-skewed) 분포를 가진 음이 아닌 정수다. Box-Cox 변환은 이러한 비정규성을 부분적으로 해결하지만(Sakia, 1992), 카운트 데이터에 내재된 바닥 효과(floor effect)를 제거하지는 못한다. 즉, 사상자가 있는 대부분의 사건은 비교적 적은 수의 부상자를 동반하며, 사상자가 많은 꼬리 부분은 상당한 노이즈를 유발하는 드문 대형 사상자 사건들로 채워진다. 이러한 분포적 제약은 두 그룹 간의 곡률 차이를 감지해야 하는 교호작용 효과를 선형 회귀 프레임워크에서 식별할 수 있는 해상도(resolution)를 제한한다.

**요약** 요약하자면, 열차 속도와 사고 유형은 각각 철도 사고의 사상자 심각도와 유의미한 연관성이 있으며, 어떤 속도 수준에서든 충돌 사건이 탈선보다 더 높은 기본 사상자 수를 발생시킨다. 그러나 열차 운행 범주를 고려하고 나면 속도가 사상자 수로 전환되는 비율은 두 사고 유형 간에 유의미한 차이를 보이지 않는다. 유의하지 않은 교호작용은 열차 종류에 의한 누락 변수 교란, PTC로 인한 고속 충돌 관측치의 희소성, 그룹 간의 제한된 공통 지지 영역, 그리고 이산형 사상자 수가 갖는 내재적 노이즈의 결합을 반영할 가능성이 높다. 이러한 연구 결과는 사상자 완화 전략이 사고 유형을 타겟으로 한 속도 정책보다는 속도 감소 및 운행 범주별 안전 개입을 우선시해야 함을 시사한다.

*References*
Barkan, C. P. L., Dick, C. T., Anderson, R., & T. R. Liu. (2003). Railroad derailment factors affecting hazardous materials transportation risk. Transportation Research Record, 1825(1), 64–74.
Cohen, J. (1988). Statistical Power Analysis for the Behavioral Sciences (2nd ed.). Lawrence Erlbaum Associates.
Evans, A. W. (2011). Fatal train accidents on Europe's railways: 1980–2009. Accident Analysis & Prevention, 43(1), 391–401.
Liu, X. (2016). Freight-train derailment rates for railroad safety and risk analysis. Accident Analysis & Prevention, 98, 1–9.
Liu, X., Saat, M. R., & Barkan, C. P. L. (2012). Analysis of causes of major train derailment and their effect on accident rates. Transportation Research Record, 2289(1), 154–163.
Rail Safety Improvement Act of 2008, Pub. L. No. 110-432, 122 Stat. 4848 (2008).
Rosenbaum, P. R., & Rubin, D. B. (1983). The central role of the propensity score in observational studies for causal effects. Biometrika, 70(1), 41–55.
Sakia, R. M. (1992). The Box-Cox transformation technique: A review. Journal of the Royal Statistical Society: Series D (The Statistician), 41(2), 169–178.


```{r}
#Casualties 가설 2 후보비교

library(ggplot2)
library(dplyr)

# ── 공통 전처리 ─────────────────────────────────────────────
# 빈 문자열 NA 처리 후 factor 변환
df_casualties <- df_casualties %>%
  mutate(
    TRNSPD    = as.numeric(TRNSPD),
    TONS      = as.numeric(TONS),
    CARS      = as.numeric(CARS),
    CARSHZD   = as.numeric(CARSHZD),
    HZD_RATIO = CARSHZD / (CARS + 1),
    PASSTRN   = ifelse(PASSTRN %in% c("N", "Y"), PASSTRN, NA),  # ← 빈 문자열 NA 처리
    PASSTRN   = factor(PASSTRN, levels = c("N", "Y"), 
                       labels = c("Freight", "Passenger")),
    TYPTRK_GRP = factor(case_when(
      TYPTRK == 1 ~ "MainLine",
      TYPTRK == 2 ~ "Yard",
      TRUE        ~ "Other"
    ))
  )

# 확인
table(df_casualties$PASSTRN)

# ── 후보 1: TYPTRK × TRNSPD (수업 형식) ─────────────────────

# Step 1: TRNSPD를 Low/High로 cut
Speed_cas <- cut(df_casualties$TRNSPD, 
                 c(min(df_casualties$TRNSPD, na.rm=T),
                   median(df_casualties$TRNSPD, na.rm=T),
                   max(df_casualties$TRNSPD, na.rm=T)), 
                 include.lowest=T, 
                 labels=c("Low Speed","High Speed"))

# Step 2: Yard 더미변수 생성 (수업 방식)
df_casualties$Yard <- rep(0, nrow(df_casualties))
df_casualties$Yard[which(df_casualties$TYPTRK == 2)] <- 1
df_casualties$Yard <- as.factor(df_casualties$Yard)

# interaction.plot (수업 방식)
interaction.plot(Speed_cas, df_casualties$Yard, 
                 df_casualties$Casualties,
                 main="Interaction: TYPTRK(Yard) × Speed on Casualties",
                 xlab="Speed", ylab="Mean Casualties",
                 legend=T)

# ggplot: y축 범위 제한으로 기울기 명확하게
qplot(x=TRNSPD, y=Casualties, data=df_casualties, colour=Yard) +
  geom_smooth(method="lm") +
  coord_cartesian(ylim=c(0, 20)) +   # ← 이상치 제외하고 확대
  labs(title="Interaction: TYPTRK(Yard) × TRNSPD on Casualties",
       x="Train Speed (mph)", y="Casualties",
       colour="Yard(1=Yes)") +
  theme_bw()

# ── 후보 2: PASSTRN × TRNSPD (수업 형식) ────────────────────
interaction.plot(Speed_cas, df_casualties$PASSTRN,
                 df_casualties$Casualties,
                 main="Interaction: PASSTRN × Speed on Casualties",
                 xlab="Speed", ylab="Mean Casualties")

qplot(x=TRNSPD, y=Casualties, data=df_casualties, colour=PASSTRN) +
  geom_smooth(method="lm") +
  coord_cartesian(ylim=c(0, 20)) +   # ← y축 제한
  labs(title="Interaction: PASSTRN × TRNSPD on Casualties",
       x="Train Speed (mph)", y="Casualties") +
  theme_bw()

# ── 후보 3: PASSTRN × HZD_RATIO (HZD_RATIO > 0) ─────────────
df_hzd <- df_casualties %>% filter(HZD_RATIO > 0)

HZD_cut <- cut(df_hzd$HZD_RATIO,
               c(min(df_hzd$HZD_RATIO),
                 median(df_hzd$HZD_RATIO),
                 max(df_hzd$HZD_RATIO)),
               include.lowest=T,
               labels=c("Low HZD","High HZD"))

interaction.plot(HZD_cut, df_hzd$PASSTRN,
                 df_hzd$Casualties,
                 main="Interaction: PASSTRN × HZD_RATIO on Casualties",
                 xlab="HZD_RATIO", ylab="Mean Casualties")

qplot(x=HZD_RATIO, y=Casualties, data=df_hzd, colour=PASSTRN) +
  geom_smooth(method="lm") +
  coord_cartesian(ylim=c(0, 20)) +
  labs(title="Interaction: PASSTRN × HZD_RATIO on Casualties",
       x="Hazardous Car Ratio", y="Casualties") +
  theme_bw()

# ── 후보 4: Cause × TONS (수업 형식) ─────────────────────────
df_cause <- df_casualties %>% filter(Cause %in% c("H","M","T"))

Tons_cas <- cut(df_cause$TONS,
                c(min(df_cause$TONS, na.rm=T),
                  median(df_cause$TONS, na.rm=T),
                  max(df_cause$TONS, na.rm=T)),
                include.lowest=T,
                labels=c("Low Tons","High Tons"))

interaction.plot(Tons_cas, df_cause$Cause,
                 df_cause$Casualties,
                 main="Interaction: Cause × TONS on Casualties",
                 xlab="Tons", ylab="Mean Casualties")

qplot(x=TONS, y=Casualties, data=df_cause, colour=Cause) +
  geom_smooth(method="lm") +
  coord_cartesian(ylim=c(0, 20)) +
  labs(title="Interaction: Cause × TONS on Casualties",
       x="Total Tons", y="Casualties") +
  theme_bw()
```
```{r}
# ── 전처리: OpenLine vs YardType ─────────────────────────────
df_casualties <- df_casualties %>%
  mutate(
    TRNSPD = as.numeric(TRNSPD),
    TYPTRK_GRP = factor(case_when(
      TYPTRK %in% c(1, 3) ~ "OpenLine",   # MainLine + Siding
      TYPTRK %in% c(2, 4) ~ "YardType",   # Yard + Industry
      TRUE ~ NA_character_
    ))
  ) %>%
  filter(!is.na(TYPTRK_GRP))  # NA 제거
# df_casualties 생성 후 바로
df_casualties <- df_casualties %>%
  filter(!is.na(PASSTRN))

cat("PASSTRN NA 제거 후:", nrow(df_casualties), "\n")
# 4,009 → 3,982행
# 분포 확인
table(df_casualties$TYPTRK_GRP)

# ── Interaction Plot (수업 방식) ──────────────────────────────
Speed_cas <- cut(df_casualties$TRNSPD,
                 c(min(df_casualties$TRNSPD, na.rm=T),
                   median(df_casualties$TRNSPD, na.rm=T),
                   max(df_casualties$TRNSPD, na.rm=T)),
                 include.lowest=T,
                 labels=c("Low Speed","High Speed"))

# interaction.plot
interaction.plot(Speed_cas, df_casualties$TYPTRK_GRP,
                 df_casualties$Casualties,
                 main="Interaction: TYPTRK_GRP × Speed on Casualties",
                 xlab="Speed", ylab="Mean Casualties",
                 col=c("red","blue"), lwd=2)

# ggplot 버전
qplot(x=TRNSPD, y=Casualties, 
      data=df_casualties, colour=TYPTRK_GRP) +
  geom_smooth(method="lm") +
  coord_cartesian(ylim=c(0, 20)) +
  labs(title="Interaction: TYPTRK_GRP × TRNSPD on Casualties",
       x="Train Speed (mph)", y="Casualties",
       colour="Track Type") +
  theme_bw()
```

```{r}
library(ggplot2)
library(dplyr)
library(car)
library(MASS)
library(ggfortify)
library(boot)

# ── Step 1: Y 분포 확인 ──────────────────────────────────────
ggplot(df_casualties, aes(Casualties)) +
  geom_density() +
  labs(title="Distribution of Casualties") +
  theme_bw()

```
심하게 right-skewed → 변환 필요

```{r}
# ── Step 2: 기본 모델 구축 ───────────────────────────────────
# Main effects only
cas.lm1 <- lm(Casualties ~ TYPTRK_GRP + TRNSPD,
              data = df_casualties)
summary(cas.lm1)
```
TYPTRK_GRP (p=0.722): YardType과 OpenLine의 평균 사상자 수 차이는 없다.
TRNSPD (p=0.039): 속도가 1mph 증가하면 사상자 +0.030명 증가. 유의하지만 효과가 매우 작음.
→ 선로 종류 자체는 사상자에 영향 없음

```{r}
# 교호작용 포함 (가설 검증 모델)
cas.lm2 <- lm(Casualties ~ TYPTRK_GRP * TRNSPD,
              data = df_casualties)
summary(cas.lm2)
```

lm2에서 TYPTRK_GRP(p=0.085)와 TRNSPD(p=0.072)가 단독으로는 유의하지 않지만 교호작용은 유의.
이건 두 변수의 개별 효과보다 함께 작용할 때 의미가 생긴다는 뜻

어떤 모델을 선택?
lm2 선택 근거:
첫째, 교호작용 텀이 유의 (p=0.026), interatcion plot  → 가설 검증에 필요
둘째, Adjusted R²가 lm1보다 높음 (0.00209 > 0.00111)
셋째, 전체 모델 F-test p값이 더 작음 (0.0098 < 0.0397)
```{r}
#partial F-test
anova(cas.lm1, cas.lm2)
AIC(cas.lm1, cas.lm2)
BIC(cas.lm1, cas.lm2)
summary(cas.lm1)$adj.r.squared
summary(cas.lm2)$adj.r.squared
```
p-value = 0.02644 <0.05

AIC, adj.r.squared -> lm2
BIC -> lm1 ?
교호작용 고려한 모델을 쓰는게 맞다
```{r}
# ── Step 3: lm2 vs lm2.so (변환 전) ─────────────────────────

cas.lm2.so <- lm(Casualties ~ TYPTRK_GRP * TRNSPD + I(TRNSPD^2),
                 data=df_casualties)
summary(cas.lm2.so)
```
adj R^2


second- order term 도입 근거

1. 도메인 지식 (가장 강력한 논리적 근거)

그래프를 그리기 전에 보고서에 쓸 수 있는 가장 확실한 공학적/물리적 근거는 바로 '운동 에너지(Kinetic Energy)' 법칙임.

물체의 운동 에너지는 속도의 제곱에 비례함

따라서 열차가 충돌하거나 탈선할 때 발생하는 파괴력(피해액이나 사상자 수에 직결되는 에너지)은 열차 속도(TRNSPD)에 비례해서 일정하게 증가하는 것이 아니라, 기하급수적(2차 곡선형)으로 증가한다고 가정하는 것이 물리적으로 매우 타당함.

2. 시각적 증거 (잔차도 및 산점도 확인)

질문한 대로 그래프를 그려보는 것이 필수적임. 단순히 TRNSPD의 그래프를 그리는 것을 넘어서, 기존 선형 모델(cas.lm2)이 설명하지 못하는 '잔차(Residual)'에 2차 곡선 형태가 남아있는지를 보여주는 것이 정석임
```{r}
library(ggplot2)

# 1. 단순 산점도와 비선형 추세선(LOESS) 확인
# 속도가 증가할수록 사상자 수가 비선형적으로 증가하는지 확인
ggplot(df_casualties, aes(x=TRNSPD, y=Casualties)) +
  geom_point(alpha=0.3) +
  geom_smooth(method="loess", color="blue") +
  labs(title="Train Speed vs Casualties", x="Train Speed (TRNSPD)", y="Casualties")

# 2. [핵심] 기존 모델(cas.lm2)의 잔차(Residual) vs TRNSPD 그래프
# 선형 모델이 속도의 영향을 완벽히 설명했다면 잔차는 0을 중심으로 무작위로 흩어져야 함.
# 만약 여기서 U자형이나 역U자형(포물선) 패턴이 보인다면, 2차항이 누락되었다는 결정적 증거임.
df_casualties$resid_lm2 <- resid(cas.lm2)

ggplot(df_casualties, aes(x=TRNSPD, y=resid_lm2)) +
  geom_point(alpha=0.3) +
  geom_hline(yintercept=0, linetype="dashed") +
  geom_smooth(method="loess", color="red") +
  labs(title="Residuals of cas.lm2 vs Train Speed", x="Train Speed", y="Residuals")
```

```{r}
# 비교
anova(cas.lm2, cas.lm2.so)   # I(TRNSPD²) 추가 효과
AIC(cas.lm2, cas.lm2.so)

```
근거 3.
AIC, partial F-test 고려 : secon-dorder term 포함된 모델 쓰는게 맞다. 

위 3가지 근거로 인해 Occam's Razor를 고려하더라도 secon-dorder term 을 포함 시키는게 낫다.


```{r}
# ── Step 4: 모델 진단 ────────────────────────────────────────
autoplot(cas.lm2.so, which=c(1,2,3,5), ncol=2, label.size=3) +
  theme_bw()
```
:선형성, 정규성, 등분산성 싹다 위반 → 변환 필요
1. Residuals vs Fitted
- 잔차가 0 근처에 몰려있고 위로 튀는 패턴
- 관측치 306이 극단적 이상치
- 등분산성 위반, 선형성 위반
2. Normal Q-Q 
- 오른쪽 꼬리가 심하게 위로 벗어남
- 306, 1982, 2507 극단적 이상치
- 정규성 심각하게 위반
3. Scale-Location 
- 추세선이 우상향 (수평이어야 함)
- 등분산성 위반 확인
4. Residuals vs Leverage 
- 표준화 잔차가 ±3 훨씬 초과 (306번: 50!)
- Leverage는 낮음 → 영향치는 아님
- 이상치(outlier)는 맞음

```{r}
# ── Step 5: Box-Cox 변환 ─────────────────────────────────────
# Y에 문제 있는지 확인 후 변환
# Y 분포 확인
ggplot(df_casualties, aes(Casualties)) +
  geom_density() +
  labs(title="Distribution of Casualties") +
  theme_bw()

# Box-Cox
boxcox(cas.lm2.so)

L <- boxcox(cas.lm2.so, plotit=F)$x[
       which.max(boxcox(cas.lm2.so, plotit=F)$y)]
cat("L =", L)

# 변환 모델
cas.lm2.so.box <- lm((Casualties^L-1)/L ~ 
                      TYPTRK_GRP * TRNSPD + I(TRNSPD^2),
                    data=df_casualties)

```

변환 전 (cas.lm2.so):  Adjusted R² = 0.003982 
변환 후 (cas.lm2.so.box):  Adjusted R² = 0.02206  ← 향상!


```{r}


# 변환 후 진단
autoplot(cas.lm2.so.box, which=c(1,2,3,5),
         ncol=2, label.size=3) +
  theme_bw() + ggtitle("Box-Cox")

# log 변환도 비교
cas.lm2.log <- lm(log(Casualties) ~ TYPTRK_GRP * TRNSPD + I(TRNSPD^2),
              data=df_casualties)
autoplot(cas.lm2.log, which=c(1,2,3,5), ncol=2, label.size=3) +
  theme_bw() +
  ggtitle("Log Transformation")


# VIF
vif(cas.lm2.so.box)
```
그나마 boxcox가 낫다.

Casualties는 양의 정수값만 갖는 이산형 변수로, Box-Cox 및 log 변환 후에도 잔차의 정규성과 등분산성 가정이 완전히 충족되지 않았다. 이는 선형 회귀 모델의 근본적 한계로, 
--> 
정규성 위반 심각 ✅ → Bootstrapping 적용 근거 있음
이산형 데이터   ✅ → 비모수 방법이 더 적절
데이터 충분     ✅ → 4,009건 (bootstrapping에 충분)

+
vif :  TRNSPD       I(TRNSPD^2) 다중공선성


```{r}
# TRNSPD I(TRNSPD^2) 다중공선성
df_casualties$TRNSPD_c <- df_casualties$TRNSPD - 
                          mean(df_casualties$TRNSPD, na.rm=T)

cas.lm2.so.box.c <- lm((Casualties^L-1)/L ~ 
                     TYPTRK_GRP * TRNSPD_c + I(TRNSPD_c^2),
                   data=df_casualties)

vif(cas.lm2.so.box.c)
```
TRNSPD와 I(TRNSPD²) 간 공선성(VIF > 10)이 관찰되었다.
Centering을 시도하였으나,
교호작용 항의 구조적 특성으로 인해 TYPTRK_GRP의 VIF가 오히려 증가하였다. 따라서 원래 스케일을 유지하되 VIF 문제를 한계로 명시한다.

통계학에서 최고차항(제곱항이나 교호작용항)이 포함된 모델의 구조적 다중공선성은 무시해도 무방하다는 것이 정설입니다. 그 이유는 다음과 같습니다.

예측력 보존: 변수들끼리 VIF가 100이 넘어가더라도, 모델 전체의 예측력(R2)이나 F-통계량은 전혀 망가지지 않습니다.

최고차항의 p-value는 정확함: 주효과(Main effect)의 p-value는 다중공선성 때문에 흔들릴 수 있지만, 우리가 관심 있는 교호작용항과 제곱항의 p-value는 다중공선성의 영향을 받지 않고 정확하게 계산됩니다.

해석의 문제일 뿐: "TRNSPD가 1 오를 때 사상자가 얼마나 느는가?"를 주효과 계수 하나만 보고 단독으로 해석하려고 할 때만 문제가 됩니다. 우리는 이미 TRNSPD 2과 교호작용이 있다는 걸 알기 때문에, 어차피 주효과를 단독으로 해석하지 않을 것입니다.

cas.lm2.so.box 채택 !

```{r}
#boxcox 변환 후 y distributuion
ggplot(as.data.frame((df_casualties$Casualties^L-1)/L), aes((df_casualties$Casualties^L-1)/L)) + geom_density()
```
"Box-Cox 변환(L=-1.5) 후 Y의 분포가 bimodal 패턴을 보였다. 이는 Casualties가 양의 정수값만 취하는 이산형 변수의 구조적 특성에서 기인하며, 어떤 변환으로도 완전한 정규분포를 달성하기 어렵다. 
그러나 변환 후 AIC가 가장 낮고(55.76) Adjusted R²가 10배 향상되어, 가용한 변환 중 Box-Cox(L=-1.5)를 최적으로 판단하였다."
```{r}
summary(cas.lm2.so.box)
```
F-test p-value < 2.2e-16 < 0.05 : 모델 유의함
TYPTRK_GRPYardType 
I(TRNSPD^2)
TYPTRK_GRPYardType:TRNSPD -> 유의 : 귀무가설 기각 가능.

```{r}
#bootstrapping 하기 전에 x transforming 해보기
# TRNSPD 분포 확인
ggplot(df_casualties, aes(x=TRNSPD)) +
  geom_histogram(bins=50) +
  labs(title="Distribution of TRNSPD") +
  theme_bw()

# X 변환 모델들 시도
# log 변환
cas.lm3.logx <- lm((Casualties^L-1)/L ~ 
                   TYPTRK_GRP * log(TRNSPD+1),
                   data=df_casualties)

# 제곱근 변환
cas.lm3.sqrtx <- lm((Casualties^L-1)/L ~ 
                    TYPTRK_GRP * sqrt(TRNSPD),
                    data=df_casualties)

# 진단 플롯 비교
autoplot(cas.lm3.logx, which=c(1,2,3,5),
         ncol=2, label.size=3) +
  theme_bw() + ggtitle("Box-Cox Y + log(X+1)")

autoplot(cas.lm3.sqrtx, which=c(1,2,3,5),
         ncol=2, label.size=3) +
  theme_bw() + ggtitle("Box-Cox Y + sqrt(X)")

# 모델 비교
AIC(cas.lm2.so.box, cas.lm3.logx, cas.lm3.sqrtx)
summary(cas.lm3.logx)$adj.r.squared
summary(cas.lm3.sqrtx)$adj.r.squared
```
Box-Cox 변환(L=-1.5) 후에도 잔차의 이산적 패턴이 관찰되어 TRNSPD에 대한 log 및 제곱근 변환을 추가로 시도하였다. 그러나 AIC 비교 결과(55.76 vs 68.00, 74.32) X 변환이 모델을 개선하지 못하였으며, 이는 잔차 문제가 X가 아닌 Y(Casualties)의 이산형 구조에서 기인함을 확인하였다. 따라서 X 변환 없이 cas.lm3을 최종 모델로 유지하였다.

```{r bootstrap_run}
# boot.result 생성
boot.fn <- function(data, index){
  fit <- lm((Casualties^L-1)/L ~ 
                      TYPTRK_GRP * TRNSPD + I(TRNSPD^2),
            data=data, subset=index)
  coef(fit)
}
set.seed(415)
boot.result <- boot(df_casualties, boot.fn, R=1000)
boot.result
```
t1*: (Intercept)
t2*: TYPTRK_GRPYard
t3*: TRNSPD
t4*: I(TRNSPD^2)
t5*: TYPTRK_GRPYard:TRNSPD
회귀 계수 추정치에 대한 신뢰도를 평가하기 위해, 부트스트래핑 기법(R=1000)을 적용하였다. 
분석 결과, 절편(t1)부터 상호작용항(t5)까지 모든 계수에서 부트스트랩 편향(Bias)이 0에 매우 가깝게 나타났다. 
이는 원본 데이터를 통해 추정된 계수들(Original)이 과적합되지 않았으며, 모집단에 대해서도 안정적이고 신뢰할 수 있는 추정치임을 입증하는 강력한 통계적 증거이다.
```{r bootstrap_ci}
# boot.result 생성 후에 실행
ci_b0 <- boot.ci(boot.result, index=1, type="perc")
ci_b1 <- boot.ci(boot.result, index=2, type="perc")
ci_b2 <- boot.ci(boot.result, index=3, type="perc")
ci_b3 <- boot.ci(boot.result, index=4, type="perc")
ci_b4 <- boot.ci(boot.result, index=5, type="perc")

boot_summary <- data.frame(
  Term     = c("β₀ (Intercept)",
               "β₁ (TYPTRK_GRPYard)",
               "β₂ (TRNSPD)",
               "β₃ (TRNSPD^2)",
               "β4 (YardType:TRNSPD)"),
  Estimate = boot.result$t0,
  
  # 아래 두 줄에 각각 ci_b4$percent[4] 와 ci_b4$percent[5] 가 추가됨
  CI_lower = c(ci_b0$percent[4], ci_b1$percent[4],
               ci_b2$percent[4], ci_b3$percent[4], ci_b4$percent[4]),
  
  CI_upper = c(ci_b0$percent[5], ci_b1$percent[5],
               ci_b2$percent[5], ci_b3$percent[5], ci_b4$percent[5])
)

boot_summary
```
H0 : B4 = 0
B4 기각 불가 -> 
"Box-Cox 변환 모델에서 TYPTRK_GRP(β₁)와 TRNSPD(β₂)는 Bootstrap 95% CI가 0을 포함하지 않아 유의하였으나, 핵심 가설인 교호작용 계수 β₃의 95% CI는 (-0.0016, 0.0058)로 0을 포함하여 귀무가설을 기각하지 못하였다. 
즉, 현재 데이터로는 YardType과 OpenLine 간 속도의 영향력 차이를 통계적으로 입증하기 어렵다."


summary()의 p-value (OLS 검정): 이 녀석은 잔차(오차)가 정규분포를 따르고, 분산이 일정하다(등분산성)는 아주 엄격한 수학적 가정 위에서 계산돼. 하지만 철도 사상자(Casualties) 데이터처럼 극단적인 이상치가 많고 비선형적인 데이터는 이 가정을 심각하게 위반해. 가정을 위반하면 OLS는 자기 확신이 너무 강해져서 표준오차(Standard Error)를 실제보다 작게 계산하고, 결과적으로 가짜 유의성(Type I Error)을 뱉어낼 확률이 높아져.

부트스트랩(Bootstrap) 신뢰구간: 반면 부트스트래핑은 정규성이나 등분산성 같은 깐깐한 가정을 하지 않아. 오직 '실제 데이터' 자체를 수천 번 섞고 뽑아서(Resampling) 경험적인 분포를 만들어내지. 따라서 이상치나 비선형성이 있는 현실 데이터에서는 부트스트랩의 결과가 훨씬 보수적이고 신뢰할 수 있는 진짜(Robust) 결과야.

🎯 2. 결론: 누구를 믿어야 할까?

당연히 부트스트랩(Bootstrap)의 결과를 믿어야 해.

Interaction plot (시각적):
YardType → 기울기 가파름 → 차이 있어 보임


[교호작용(Interaction Term) 검증 및 최종 결론 도출]
선로 종류(TYPTRK)와 속도(TRNSPD) 간의 교호작용을 검증하기 위해 단계적 모델링을 수행함. 주효과 모델(lm1), 교호작용 추가 모델(lm2), 속도 제곱항 추가 모델(lm2.so)을 비교한 결과 lm2.so의 성능이 가장 우수했음.

그러나 lm2.so의 잔차 플롯에서 정규성 및 등분산성 위배가 발견되어, Box-Cox 변환을 적용한 lm2.so.box를 최종 모델로 선정함. OLS 요약 통계량(Summary) 분석 결과 교호작용 텀의 p-value가 0.029로 도출되어 유의수준 0.05 하에서 귀무가설을 기각할 수 있는 것으로 나타남.

하지만 OLS의 한계와 교차 검증:
Box-Cox 변환 이후에도 잔차의 이분산성이 일부 남아있는 점을 고려할 때, OLS의 p-value가 과대 추정(Type I Error)되었을 위험성이 존재함. 이를 검증하기 위해 잔차 가정으로부터 자유로운 부트스트랩(Bootstrap, R=1000) 기법을 추가로 적용함. 그 결과, 교호작용 텀에 대한 95% 신뢰구간에 0이 포함되는 상반된 결과를 확인함.

최종 결론:
본 분석은 잔차 가정이 위배된 상황에서 OLS의 결과보다 부트스트랩의 비모수적 추정치가 더 안정적이고 신뢰할 수 있다고 판단함. 따라서 최종적으로 귀무가설을 기각하지 않음. 이는 사상자 수에 미치는 속도의 영향력이 선로 종류(Main vs Yard 등)에 따라 통계적으로 유의미한 차이를 보이지 않음을 시사함. 사상자 수 폭증의 핵심 원인은 선로 종류 간의 차이가 아니라 **고속 운행 자체의 기하급수적 위험성(속도의 제곱항)**에 있음.
```{r}
#bootstrap의 결론을 테스트 하기 위해 test set 도입
source("TestSet.R")

## set test sets size:
test.size <- 1/3

## generate training sets and test sets from df_casualties data:
# 수업 코드의 test.set 함수 활용
casualties.data <- test.set(df_casualties, test.size)

## Build models with training set:
# Model 1: 교호작용이 없는 모델 (주효과 + 2차항)
cas.lm.no_inter.train <- lm(Casualties ~ TYPTRK_GRP + TRNSPD + I(TRNSPD^2), 
                            data=casualties.data$train)

# Model 2: 교호작용이 있는 모델 (기존 lm2.so 구조)
cas.lm.inter.train <- lm(Casualties ~ TYPTRK_GRP * TRNSPD + I(TRNSPD^2), 
                         data=casualties.data$train)

## Recall that we need to measure predicted MSE. 
## First, how to predict with lm models:
cas.lm.no_inter.pred <- predict(cas.lm.no_inter.train, newdata=casualties.data$test) 
cas.lm.inter.pred <- predict(cas.lm.inter.train, newdata=casualties.data$test)

## Next, compute PMSE:
# 수업 코드의 mse 함수 활용
pmse.no_inter <- mse(cas.lm.no_inter.pred, casualties.data$test$Casualties)
pmse.inter <- mse(cas.lm.inter.pred, casualties.data$test$Casualties)

# 반복 (Multiple Iterations)
# Run multiple iterations with different testing and training sets

## create vectors to store PMSE
pmse.no_inter.result <- NULL
pmse.inter.result <- NULL

# 결과를 재현할 수 있게 시드 고정 (수업 코드에는 없지만, 보고서를 위해 추가하는 것을 추천함)
set.seed(415)

for (i in c(1:20)){
  # set test sets size: 
  test.size <- 1/3
  
  # generate training sets and test sets from df_casualties data:
  casualties.data <- test.set(df_casualties, test.size)
  
  # Build model with train set:
  # Model 1: 교호작용이 없는 모델 (파란선)
  cas.lm.no_inter.train <- lm(Casualties ~ TYPTRK_GRP + TRNSPD + I(TRNSPD^2), 
                              data=casualties.data$train)
  
  # Model 2: 교호작용이 있는 모델 (빨간선)
  cas.lm.inter.train <- lm(Casualties ~ TYPTRK_GRP * TRNSPD + I(TRNSPD^2), 
                           data=casualties.data$train)
  
  # Predict with lm models:
  cas.lm.no_inter.pred <- predict(cas.lm.no_inter.train, newdata=casualties.data$test) 
  cas.lm.inter.pred <- predict(cas.lm.inter.train, newdata=casualties.data$test) 
  
  # Compute PMSE:
  pmse.no_inter <- mse(cas.lm.no_inter.pred, casualties.data$test$Casualties)
  pmse.inter <- mse(cas.lm.inter.pred, casualties.data$test$Casualties)
  
  # Add the PMSE for this run into your vector
  pmse.no_inter.result <- c(pmse.no_inter.result, pmse.no_inter)
  pmse.inter.result <- c(pmse.inter.result, pmse.inter)
}

## Compare models based over 20 runs of PMSE

# Plot results (y축 범위를 두 모델의 최소~최대치로 맞춰서 그래프가 안 잘리게 개선함)
# ggplot2 패키지 로드
library(ggplot2)

# 1. x축으로 사용할 Index 생성 (20번 반복했으니 1~20)
Index <- 1:length(pmse.no_inter.result)

# 2. ggplot에서 그리기 쉽게 데이터 프레임으로 묶기
df <- data.frame(
  Index = Index,
  pmse_no_inter = pmse.no_inter.result, # 교호작용 없음 (파란선)
  pmse_inter = pmse.inter.result        # 교호작용 있음 (빨간선)
)

# 3. ggplot으로 시각화 (요청한 형식 그대로 적용)
ggplot(data=df, aes(x=Index)) +
  # 파란선: 교호작용 없는 모델
  geom_line(aes(y = pmse_no_inter), color = 'blue', size = 1) +
  # 빨간선: 교호작용 있는 모델 (twodash 점선 처리)
  geom_line(aes(y = pmse_inter), color = 'red', linetype = 'twodash', size = 1) +
  
  # 제목 및 축 이름 설정
  ggtitle("Model Comparison Based on PMSE (Casualties)") +
  ylab("PMSE") + xlab("Iteration") +
  
  # 제목 가운데 정렬
  theme(plot.title = element_text(hjust = 0.5), 
        plot.caption = element_text(hjust = 0.5))


```
[Test-set 분리를 통한 교호작용 항 예측력 검증 및 최종 결론]
OLS 기반의 모델 요약 통계량에서 도출된 교호작용 항의 유의성(p=0.029)이 과적합(Overfitting)에 의한 가짜 신호인지 검증하기 위해, Train/Test 셋(2:1) 분할 교차 검증을 20회 반복 수행하여 예측 평균 제곱 오차(PMSE)를 비교하였다.

시각화 결과, 교호작용 항이 포함된 다항 모델(Red Line)과 주효과 및 2차항만 포함된 단순 다항 모델(Blue Line)의 PMSE 궤적이 20회의 반복 동안 사실상 완벽하게 일치하는 것으로 나타났다. 이는 선로 종류와 속도 간의 상호작용 변수가 새로운 사고 데이터의 사상자 발생 규모를 예측하는 데 어떠한 실질적인 정보 획득(Information Gain)이나 오차 감소도 제공하지 못함을 명백히 보여준다.

최종 결론:
엄격한 모수적 가정을 배제한 부트스트래핑(Bootstrap)의 95% 신뢰구간 평가와, 실증적인 Test-set 반복 예측 검증이라는 두 가지 강력한 교차 검증 결과가 모두 '교호작용 무의미'를 지시하고 있다. 따라서 본 연구는 잔차 가정이 위배된 OLS의 p-value를 기각하고,
**"사상자 수에 미치는 속도의 영향력은 선로 종류에 따라 차이가 없다(H0:β4=0 기각 불가)"**
는 결론을 최종 확정한다.
